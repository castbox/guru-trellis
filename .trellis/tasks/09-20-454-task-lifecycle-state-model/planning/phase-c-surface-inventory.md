# Phase C Surface Inventory

## 1. Ownership decision

Phase C采用“fixed Fork framework prerequisite + Guru-owned substrate”单一路径。

| Surface | Current owner | Phase C disposition | Write repository |
| --- | --- | --- | --- |
| `.trellis/scripts/common/task_store.py` | `castbox/Trellis` Fork | 修改official create/rename/archive primitive | `castbox/Trellis` |
| `.trellis/scripts/common/task_utils.py` | `castbox/Trellis` Fork | 修改TaskId/TaskRef resolution primitive | `castbox/Trellis` |
| `.trellis/scripts/common/session_storage.py` | `castbox/Trellis` Fork | 替换为path-free common-dir session schema | `castbox/Trellis` |
| `.trellis/scripts/common/active_task.py` | `castbox/Trellis` Fork | 按TaskId/generation解析，停止path authority/repoint | `castbox/Trellis` |
| `.trellis/scripts/task.py` | `castbox/Trellis` Fork | official CLI wiring与focused tests | `castbox/Trellis` |
| `trellis/skills/guru-team/contracts/task-lifecycle/**` | Guru canonical | 新增shared DTO/schema primitives | `castbox/guru-trellis` |
| `trellis/skills/guru-team/runtime/task_lifecycle/**` | Guru canonical | 新增identity/checkout/branch/ledger runtime | `castbox/guru-trellis` |
| Phase C `guru-*` packages | Guru canonical | 新增package-ready inactive contracts | `castbox/guru-trellis` |
| registry/workflow/active manifest/installed/platform bytes | #434 activation owner | Phase C禁止修改 | none in Phase C |

Guru ownership inventory不声明`.trellis/scripts/**`。Phase C不得用overlay、installer post-patch、copied module或
alternate task/session store绕过Fork owner。

## 2. Fork prerequisite inventory

Current fixed source：`castbox/Trellis@43fffc170927c85d9f7fc106cc5a059e80d4530b`。

Fork implementation work item在实施前fresh创建；本Planning不虚构Issue id、branch或candidate commit。其closed
scope严格覆盖：

- immutable `task.json.id`；
- create generation `0`与legacy missing-generation normalization；
- rename只改TaskRef/display/back-reference；
- archive保持TaskId/source/generation；
- path-free common-dir session payload `task_id + lifecycle_generation`；
- TaskId -> current TaskRef fresh resolution；
- init/update template/extractor/generated byte consistency；
- focused CLI/Python/golden regression。

Fork不拥有Guru branch association、checkout acquisition、resource ownership ledger、semantic Skill gate或
workflow activation。

## 3. Guru substrate modules

Planned canonical root：

```text
trellis/skills/guru-team/contracts/task-lifecycle/
trellis/skills/guru-team/runtime/task_lifecycle/
```

Planned runtime module split：

| Module | Responsibility | Forbidden responsibility |
| --- | --- | --- |
| `identity.py` | TaskId/TaskRef/TaskLifecycleKey validation与resolution adapter | session、branch、checkout、ledger mutation |
| `source.py` | issue/no-Issue source与delivery target normalization | Closure disposition |
| `git_facts.py` | current common-dir/worktree/ref facts | durable authority |
| `checkout_acquisition.py` | adopt/provision plan与transaction | session/branch authority |
| `checkout_resolution.py` | live candidate/result/selection validation | candidate persistence |
| `branch_store.py` | common-dir current association store | checkout path、HEAD、ownership |
| `branch_resolution.py` | establishment candidate与four-state recovery | rebind history migration |
| `rebind.py` | two-route mutation、rollback与recovery | stash/merge/rebase/cherry-pick/reset |
| `session_adapter.py` | Fork session DTO read/write adapter | alternate session store |
| `resource_ledger.py` | acquisition/retirement/ownership/responsibility state | Cleanup semantic judgment |
| `results.py` | ResultRef/TransactionRef/ResourceSeal/CleanupRef primitive validation | package semantic decision |
| `errors.py` | stable substrate error identities | route selection |

每个非生成module在提交前执行3000-line检查。达到阈值时按上表职责拆分，不建立`common.py`总仓库。

## 4. Phase C public packages

| Package | Judgment mode | Phase C output boundary | Activation owner |
| --- | --- | --- | --- |
| `guru-create-task` | semantic | created/recovery/refresh/blocked package contract | Phase E434 |
| `guru-establish-task-identity` | semantic | established/selection/conflict/blocked | Phase E434 |
| `guru-establish-task-branch-binding` | semantic | established/selection/recovery/invalid/blocked | Phase E434 |
| `guru-ensure-task-checkout` | semantic | resolved/selection/provision/recovery/blocked | Phase E434 |
| `guru-rebind-task-branch` | semantic | rebound/already-bound/reconcile-required/recovery/blocked | Phase E434 |
| `guru-activate-task` | deterministic | activated/session-binding-recovery-required/blocked | Phase E434 |

Phase C创建canonical package和package-owned consumer schemas，但不写registry selector、active manifest、installed
copy、platform projection或workflow target。Package existence不等于production capability。

## 5. Consumer boundary

- Phase D0消费operation-scoped Git facts，修正Reconcile、Task Commit与Branch Review的stage-evidence lineage；它不
  消费或写入durable task identity，也不创建新的generic evidence store；
- Phase D443消费TaskLifecycleDTO、Fork session primitive与TaskId -> TaskRef resolver；
- Phase D436消费TaskLifecycleKey、branch/resource/result primitives；
- Phase E434消费package-ready interfaces并独占workflow/registry/manifest/projection activation；
- #410保留完整Release matrix；Phase C只运行focused validation。

### 5.1 Phase D0 stage-evidence ownership

| Surface | Owner | D0 contract | Forbidden carry-over |
| --- | --- | --- | --- |
| selected base / current task HEAD / merge-base | live Git facts | pair guard每次fresh解析，old base只在当前operation由唯一merge-base派生 | tracked/session/general DTO `base_head` authority |
| pre-review reconciliation commit | `guru-reconcile-task-base` | compatible `post_plan/post_check/post_commit`创建expected-head-bound双亲merge commit | no-commit `reconciled`后直接进入Task Commit/Branch Review |
| committed implementation candidate | `guru-create-task-commit` | 输出exact `branch_review_commit`与portable base ref，不构造base pair | caller补造old/new base SHA |
| first/full review | `guru-review-branch` full profile | selected base必须是review HEAD祖先，审查committed `base...HEAD` | current base未入history时直接triple-dot审查 |
| bounded continuity | Reconcile + `guru-review-branch` continuity profile | 只承接prior full review，验证prior review/new base ancestry与candidate tree identity | `post_commit`或首次review使用continuity |

#459/#460/#462仍属于current predecessor maintenance：D0只在计划与测试中登记其target行为保证。日期前缀TaskRef
由D436 target resolver承接；created Issue provenance、schema一致性与same-result recovery由Phase C `guru-create-task`
candidate及E434 cutover承接。D0不得复制旧workspace mapping、nested result/digest或legacy TaskId推断。

## 6. Zero-copy and zero-second-authority checks

Phase C validation必须证明：

- Guru diff无`.trellis/scripts/**` production patch；
- Guru runtime未复制Fork task/session module；
- no `task_workspace` domain object、workspace mapping reader/writer或absolute-path session字段进入新module；
- no durable task identity index；
- no alias、dual-read、dual-write或compatibility shim；
- branch association、session pointer与resource ledger分别只有一个store owner。

## 7. Current task execution admission

Phase C package inventory描述的是target-native candidate，不是当前task的bootstrap authority。当前task进入Phase 2前
必须继续消费current production owner：

- current-session association只由现有正式session owner证明；future `guru-activate-task`、Fork path-free session
  primitive或Phase D443 Bind major不得提前用于当前task自举；
- session binding缺失且现有owner无法通过entry preconditions时，当前task保持planning并返回
  `planning_execution_blocked`；禁止直接写common-dir session record、复制旧session、修补legacy mapping或task
  metadata；
- approved Planning bytes先形成独立local planning checkpoint commit，随后post-plan pair guard与
  `guru-reconcile-task-base`在planning状态完成base reconcile；
- 只有`reconciled(resume_target=task_activation)`被正式activation owner消费并产生checked `activated` result后，
  Phase C implementation才开始。

上述admission步骤不属于Phase C交付surface，不改变Phase D/E owner边界，也不把current predecessor提升为target
contract。
