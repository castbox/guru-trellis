# #353 实施计划

## Phase 1: Planning Gates

- [x] 读取 live Issue #353、当前 `origin/main` 及 Finalizer current contract/spec。
- [x] 复核 #342、#344、#347、#350 的已交付边界，确认不吸收既有实现。
- [ ] 完成 task-owned RDT contribution 规划与 wording review。
- [ ] 完成 Architecture `task_impact_sync(stage=planning)`；保持 `no_architecture_impact` 假设，除非 live baseline 给出冲突。
- [ ] 完成 `guru-qualify-normal-scenario` 与 `guru-qualify-solution-mechanism` 的 planning candidate qualification。
- [ ] 运行 `guru-approve-task-plan`，获得 approved；随后由 workflow 单独确认并执行 task activation。

## Phase 2: Implementation

1. 在 canonical Finalizer runtime 中定位旧 ordinary transaction、provenance-tail validator、base-evolution classifier 和 reprepare route 的实际调用顺序。
2. 增加窄的 current-plan reprepare/rebind 分类：只接受同 task/repo/base/branch/scope 的旧未绑定 transaction 与一个已验证的合法 tail；不得修改现有 tail allowlist 或 generic adoption predicate。
3. 让 preview 返回已有 `reprepare_required`/`reprepare_preview` contract 所需的最小 owner handoff；确认旧 payload 不会覆盖当前 Publication authority。
4. 在首个剩余 mutation 前持久化同一 owner transaction 的 current-plan binding，并复用现有 recovery engine；为 interruption/retry 增加零重复 mutation 断言。
5. 仅在语义合同确实变化时更新 Finalizer references、workflow data-contract/quality spec 与 task-owned RDT；保持 schema/public I/O 不变优先。
6. 通过 `trellis-before-dev` 重新读取当前 spec 与 curated task context 后开始代码修改。

## Validation

- canonical Finalizer targeted source tests。
- installed Finalizer targeted package tests。
- real-topology direct-tail/base-evolution/composed-tail positive and negative integration tests。
- `trellis/presets/guru-team/scripts/bash/apply.sh --repo . --all-platforms`。
- canonical/installed/dogfood/platform byte parity and ownership checks。
- `trellis/presets/guru-team/scripts/bash/check-dogfood-overlay-drift.sh`。
- package/task validation, `git diff --check`, recursive `.new`/`.bak` count equals zero, and sidecar scan.
- Phase 2 `guru-check-task` semantic pass before task commit.
- fresh full-diff Branch Review before publication readiness.

## Risk And Stop Conditions

- Stop if a public DTO, typed exit, transaction schema/mode/stage, or cross-skill consumer must change; route through scope/contract review first.
- Stop if the positive topology requires hand-editing transaction files, weakening provenance validation, or mutating PR #337/#333 state.
- Stop if canonical-to-installed projection cannot converge without `.new`/`.bak` residue or if any platform copy diverges.
- Commit, push, PR mutation, merge, Issue closure, and cleanup remain separate confirmations.

## Deliverables

- canonical Finalizer runtime and focused tests;
- synchronized contract/spec/RDT artifacts only where required by the reviewed behavior;
- installed/dogfood/platform projections generated by the supported preset path;
- complete Phase 2 and Branch Review evidence, with unverified boundaries stated explicitly.
