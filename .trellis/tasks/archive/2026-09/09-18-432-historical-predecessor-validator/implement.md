# Implementation Plan

1. 参数化 shell wrapper 的 historical inputs，并允许 `existing` mode。
2. 从 immutable before tag tree 派生并校验 predecessor extension/CLI identity。
3. 实现单 cell `existing` runner 与精确 summary。
4. 绑定 official update 后的 active workflow managed identity，并在 marketplace preview/force 前阻断未知或用户修改字节。
5. 增加 wrapper 转发、两个 predecessor mapping、单 cell cardinality、post-update identity mismatch 与既有模式不回归测试。
6. 更新 preset maintainer 文档，运行 targeted tests、preset reapply、source/installed validators、projection/ownership/drift 和 residue checks。
