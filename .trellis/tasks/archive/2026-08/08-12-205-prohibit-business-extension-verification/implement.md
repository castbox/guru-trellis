# Implementation Plan

## Phase 1: Public Contract Migration

- [ ] 更新 global workflow graph，删除 verifier mandatory invocation 与 Finalizer `verification_required` consumer edge，重新计算 invoke/exit/target counts。
- [ ] 将 Finalizer aggregate/gate/transaction 切换到 6.0/4.0/2.0，删除两个 verifier re-entry profiles、一个 exit、consumer projection、schemas/examples active inventory。
- [ ] 将 verifier aggregate/private result 切换到 4.0，建立单一 source-owned standalone profile 与 `verified|blocked` exits。
- [ ] 更新 registry、production-current contract、extension manifest 和 graph validators，使 standalone-only active package不要求 global workflow marker。
- [ ] 标注旧 input/output/private schemas 为 immutable legacy，并加入稳定拒绝与 reprepare migration contract。

## Phase 2: Runtime And Recovery Removal

- [ ] 删除 changed-path marketplace prefixes、candidate surfaces 与 Finalizer `marketplace.required` route calculation。
- [ ] 删除 Finalizer verification transition、owner checker bridge、verification ref、task-local artifact读取、archive handling 和旧 recovery route。
- [ ] 将 content push 后的 deterministic transition 接到 Draft PR/archive continuation，保持 Publication freshness、remote HEAD、PR identity 与 confirmation gates。
- [ ] 删除 verifier task/workflow identity、task-local recorder/checker path、business target checkout 和 task finding return。
- [ ] 增加 source checkout preflight，在 clone/install/temp/artifact write 前校验 canonical repo、origin、ref、resolved commit、HEAD、clean state 与 forbidden task fields。
- [ ] 保留 clean throwaway executor、capability evidence、semantic adequacy、redaction 与 source-only ignored session state。

## Phase 3: Tests And Evals

- [ ] 更新 Finalizer/verifier package contract tests，覆盖 current unions、legacy rejection、unique consumers 与 private-state lifecycle。
- [ ] 更新 runtime tests，覆盖 direct Finalizer continuation、旧 verify transaction reprepare、非 source zero-side-effect拒绝与 source clean throwaway success。
- [ ] 重写 finish-family integration 与 native eval，删除 workflow verifier chain、`not_required` bridge 和 task artifact staging。
- [ ] 建立 representative business fixture matrix，覆盖 docs、code、config、`.trellis/**`、平台副本与 installed manifest，断言 verifier commands/artifacts 为零。
- [ ] 保留并迁移 source-repo clean install、existing repo update、Trellis update/reapply、platform equality、ownership、redaction 与 sidecar cases。
- [ ] 加入 current-surface banned-reference audit，阻止 task-bearing verifier profile、Finalizer verifier projection 和 path applicability 回归。

## Phase 4: Documentation And Distribution

- [ ] 更新 workflow、skill-package、data-contract、companion、quality、preset、upstream-ownership 与 public-docs SSOT。
- [ ] 更新 workflow README、preset README 与 Codex/Claude/Cursor finish entry 文案，区分业务 closeout 和 source standalone verification。
- [ ] 运行 preset `apply.sh --repo .` 同步 `.trellis/guru-team/` 与 shared/Codex/Claude/Cursor package copies。
- [ ] 处理全部 `.new`/`.bak`，运行 dogfood overlay drift 与 canonical/generated byte identity checks。

## Phase 5: Validation

- [ ] 运行两个 package tests、Skill graph tests、finish-family integration、runtime tests、preset installer tests 与 production eval。
- [ ] 运行 JSON schema、Python compile、shell syntax、task validation 和 `git diff --check`。
- [ ] 运行 clean throwaway workflow/preset install、existing repo upgrade/update、official Trellis update/reapply 与 zero-sidecar validation。
- [ ] 运行代表性业务 fixture完整 closeout smoke，并保存 verifier invocation/artifact 为零的测试证据。
- [ ] 执行 `guru-check-task` 完整 Phase 2 semantic gate。
- [ ] 执行独立 current-HEAD Branch Review，要求零未关闭 P0-P3 finding。

## Expected Commands

```bash
python3 -m unittest trellis.skills.guru-team.packages.guru-finalize-task.tests.test_contract
python3 -m unittest trellis.skills.guru-team.packages.guru-verify-extension-installation.tests.test_contract
python3 -m unittest trellis.skills.guru-team.tests.test_finish_family_integration
python3 trellis/workflows/guru-team/scripts/python/test_guru_team_trellis.py
python3 trellis/presets/guru-team/scripts/python/test_apply_guru_team_trellis_preset.py
trellis/presets/guru-team/scripts/bash/apply.sh --repo .
trellis/presets/guru-team/scripts/bash/check-dogfood-overlay-drift.sh
trellis/presets/guru-team/scripts/bash/verify-throwaway-install.sh
python3 ./.trellis/scripts/task.py validate .trellis/tasks/08-12-205-prohibit-business-extension-verification
git diff --check
```

## Rollback Points

- workflow graph、Finalizer schemas、verifier schemas、runtime、registry 与 manifest 作为一个版本单元回滚。
- generated copies 只从回滚后的 canonical source 重新 apply，不保留 current/legacy 混合安装状态。
