# #305 Requirements 审核与修订映射

## Authority

- Change authority：[GitHub Issue #305](https://github.com/castbox/guru-trellis/issues/305)。
- Requirements planning baseline：
  `f962b640f7f142bf657fc9e5ebf5da3cd79a1a6b`；这是本 task 的历史分支基线，不是当前候选
  HEAD。Requirements gate 时的 frozen candidate 绑定
  `HEAD=2bd293d8d333d399b05ca76c0393c7b0d49c13f3` 与当时展示的十一个 Requirements/入口投影修订文件；
  Phase 1 随后新增的 Design/Test/Architecture/task-planning 文件不被倒算进该 Requirements evidence。
  当前 worktree candidate 由 live file set/content 定位，不再用固定文件数量冒充 freshness；HEAD 的提交
  时间为 `2026-08-25T06:37:25+08:00`，仅用于定位分支基线，不作为 current authority 或 evidence，
  也不在本 task 内改写历史。本轮 pre-prerequisite authority snapshot 曾通过 fresh reconciliation 绑定
  `origin/main=d907fcc5e17f23b6499648e5e9a208457f2d6f8b`（提交时间
  `2026-08-25T17:20:17+08:00`），该 ref 与 `.40` 只保留为历史 evidence。selected post-merge base
  `5650df47fe17fe89b7cb616be6c9551608164832` 现已由 live `origin/main` 绑定，并包含 #311 PR #313/
  merge `21c7da14798683193b460a5e7c5bd24c7c517804`、#312 PR #314/merge `3efcce72…`、#267
  release-authority merge `a41b8a34…`、evidence-fix PR #316 merge `9f560ec1…` 与 installed
  platform-selection preservation PR #317 merge `736ef333…` 与 caller-inventory consistency PR #318 merge
  `5650df47…`；本 task worktree 未 rebase/merge，但本轮已从
  该 exact base fresh 读取 `.42` authority，确认 `.42` 相对 `.41` 只提升 authority/release facts、
  `a41b8a34...9f560ec1` 只修正 lifecycle evidence、dogfood provenance 与 archive/merge facts，而
  `9f560ec1...736ef333` 是 material current behavior advance：Finalizer reprepare 精确保留 parent manifest
  platform selection，invalid identity 在 source checkout、preset apply 与 commit 前 fail closed，并把 latest
  stable current fact 修正为 `.2/.38/CLI 0.6.15`；`736ef333...5650df47` 只刷新 Finalizer second-hop
  caller-inventory anchor、dogfood provenance、task archive 与 PR #318 merge facts，不改变 runtime behavior、
  public Skill I/O/schema/typed exit、`.42` authority 或 Release identity；`.3/.39` 仍是 unverified successor。current-to-target rebind、
  84/34/13/50 zero-loss projection 与 Design/Test/Architecture/task planning projection 已重建，fresh
  Requirements semantic、Strict technical 与确定性闭包审核已针对同一 exact identity 通过。上一 selected base `736ef333…`、`9f560ec1…`、`3efcce72…` 与更早 snapshot
  `a4b68d42b25e3d2173fac2db353295043590cca5` 只保留为历史 evidence，不再作为
  current locator。
  每轮 gate 必须只读核对 live upstream drift；在后续阶段的独立 workflow 入口明确之前不
  rebase/merge，也不得把历史 ref、current authority 与 working-tree candidate 拼接成虚构 evidence。
- Repository Requirements authority：`docs/requirements/evolution/README.md`、
  `requirement-main.md`、`requirement-non-functional.md`、
  `current-capability-inventory.md`。
- 本文件只记录本 task 的 review/revision delta 与执行映射，不复制或替代 repository
  Requirements SSOT。

## Current Phase Boundary

- `EVO-EVD-044..051` / `REQ-REV-133..142` 是 material Requirements scope refinement：#311/#312 必须先完成并
  合入 Evolution selected base，Evolution 再从 exact merge/base identity fresh rebind RDT、Architecture 与
  capability inventory，并证明两项 current 能力和全部既有能力的 successor 差集为 0。当前 selected base
  已包含两个 prerequisite merge、`.42` fact-only authority promotion、PR #316 fact-only evidence correction 与
  PR #317 material platform-selection preservation 与 PR #318 fact-only inventory/provenance correction；六维 prerequisite live classification、`.42`
  RDT/Architecture/inventory rebind、merged behavior reconciliation 与 84 requirements / 34 NFR / 13 target
  deltas / 50 fixtures zero-loss projection 已完成，fresh Requirements semantic、Strict technical 与确定性闭包
  审核已通过，P1/P2/P3 finding 与 high-risk open question 均为 0。绑定 `REQ-REV-011..132` 的 Requirements ready、`DES-REV-001..014` 以及
  pre-`REQ-REV-142` `DES-REV-001..052` Design review 结论均已失效。
- 当前 continuation 停留在 Phase 1 Design gate closure，repository Requirements/trace 状态为
  `requirements_ready_for_design` / `requirements_trace_ready_for_design`；#311/#312 merge/lifecycle、authority rebind、merged behavior
  reconciliation、Requirements-stage zero-loss projection 与 fresh Requirements 双审均已 current。
  Evolution Design/Test/Architecture/task 候选已同步 planning projection，fresh Design 全稿审核与确定性闭包
  已针对同一 exact candidate 通过，P1/P2/P3 finding 与 high-risk open question 均为 0。
- `task.json` 的 `in_progress` 是此前 activation 留下的 task lifecycle state，不是当前 Phase 2 或 runtime
  实现授权。最新明确用户决定和本 task planning projection 将当前工作收窄为 Phase 1 Design gate closure，
  并明确停在 fresh Design review 通过后的 planning boundary；不执行 `task.py start` 或 Evolution runtime implementation。
- `docs/design/evolution/`、`docs/test/evolution/`、task-owned Architecture contribution 与 task
  `design.md`/`implement.md` 已同步为 `fresh_design_review_passed` / `evolution_refactor_eligible` planning
  projection；#312 归
  Reconcile，#311 publication 归 Publish/Finish/Merge，standalone verifier failure 归 Projection，且不新增
  public Skill identity、lifecycle owner、target delta 或 Design responsibility；PR #317 平台保真由既有
  Publish/Finish/Projection responsibility 承接。
- Repository Design/Test 是 target authority；task 三件套只引用 authority locator 并记录 #305 的
  delta、reconciliation、执行/验证/交付映射，不构成平行 RDT。
- pre-`REQ-REV-142` 的历史 `design_ready_for_delivery_planning` / `fresh_design_review_passed` 仍不能作为
  current gate、delivery predecessor 或 merge 后审查证据；同名 current 结果只绑定本轮
  `origin/main@5650df47…` exact candidate 与当前 fresh Design closure。
- 不执行 runtime implementation、commit、push、PR、merge、tag、Release、base sync、
  Issue 链修改或 Git submodule 操作；不执行新的 lifecycle 副作用，不修改 shared current RDT/Architecture，
  也不改 official Trellis source/npm/cache。
- #304 是相关但隔离的 pre-refactor Release authority，不是本 Requirements gate 的前置或 owner。

## Review And Revision Delta

| Finding | Severity | Repository authority gap | Required closure | Status |
| --- | --- | --- | --- | --- |
| `REQ-REV-001` | P1 | README 把 #304 发布闭环写成 Design 前置，并把 `candidate_stale` 快照写成已冻结/验证 | 统一 #304 隔离、snapshot 状态与 Design eligibility | closed in Requirements revision |
| `REQ-REV-002` | P1 | Design 启动条件被列入 `requirements_ready_for_design` 文档门禁，形成文档资格与后续副作用资格的循环 | 分离文档资格与后续副作用资格及 owner-scope contract | closed in Requirements revision |
| `REQ-REV-003` | blocking P2 | `.40` Test capability 只给出汇总式“有 target fixture”结论，不能逐组复核 successor 差集 | 为全部 `TST/SCN/CASE` 建立 current capability、target requirement 与 fixture successor | closed in capability inventory revision |
| `REQ-REV-004` | blocking P2 | migration/provider 的正常失败、部分成功和 terminal state 不足以约束 Design | 补齐 live reread、无重复副作用、无 mixed graph 与 forward recovery 验收 | closed in Requirements/NFR/fixture revision |
| `REQ-REV-005` | P3 | “Requirements 阶段不创建资源”没有区分当前审核 task 与后续实施资源 | 收窄为不据 target authority 创建后续实施资源 | closed in Requirements revision |
| `REQ-REV-006` | blocking P2 | provider 恢复未要求绑定 current 外部合同，也未闭合可重试/幂等/有界停止语义 | 要求 action contract/capability binding、live reread、安全重复前提、有界尝试和 non-retryable/partial-success stop | closed in Requirements/NFR/fixture revision |
| `REQ-REV-007` | blocking P2 | migration 使用 cutover 前后分支，但 phase identity 与 cutover boundary 不可观察 | 要求 preflight/application/final-validation identity、唯一 cutover boundary 和逐阶段失败分类 | closed in Requirements/NFR/fixture revision |
| `REQ-REV-008` | P3 | API/CLI 不适用理由明确，但只笼统引用“需求标准” | 显式引用 `requirement-doc-standard` 第 5.1 节 | closed in Requirements revision |
| `REQ-REV-009` | blocking P2 | README 重复定义 #304 隔离、snapshot 状态与 Design 资格规则，和 `requirement-main.md` 形成可再次漂移的双主定义 | 将 README 收敛为 status/locator 导航，只链接第 0/10 章主定义 | closed in Requirements revision |
| `REQ-REV-010` | P3 | 第二章入口摘要未显式列入 `REQ-UC-EVO-033` 正常清晰请求 | 将该场景归入标准 change workflow 的摘要级入口范围 | closed in Requirements revision |
| `REQ-REV-011` | P1 | 第二章声明五类入口，但第六章只保留 standard/task-free；`github_pr`/`none`、Issue closure 与 specialist/release/resume route 不可达 | 重构第六章入口分流、delivery route selection/freshness、closure-current/not-applicable 与全部 terminal/re-entry | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-012` | P1 | post-publish smoke 被写成发布前置，Release 时序不可执行 | 分离 pre-publish gate、独立确认、immutable publication、tag-pinned post-publish verification 与发布后 forward recovery | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-013` | blocking P2 | task-free 资格是循环定义，且缺少 finding/recheck、位置/active-task recovery 与 blocked | 定义显式/自动选择边界、一次 choice、完整 check/revision/recovery/blocked/scope-expansion 产品结果 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-014` | blocking P2 | Phase 2 后已有本地副作用时，latest-intent override 没有保留、suspend、清理或新 lifecycle 路线 | 按无副作用、可逆本地副作用、不可逆远端副作用定义 override/additive route 与 fixture | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-015` | blocking P2 | 执行连续性零计数与 migration terminal 在功能/NFR 两边重复主定义 | 功能正文拥有行为与状态，NFR 只拥有可测质量门槛并单向引用 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-016` | blocking P2 | fresh review 发现 entry/change-mode、Release confirmation、Issue closure 与 specialist caller/terminal 使用未映射组合状态 | 收敛为两级 entry projection、`ready_for_release_confirmation`、两个 exact closure outcome 与 caller/standalone 互斥出口 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-017` | blocking P2 | migration、resume 与可逆本地 work 只覆盖成功/保留，没有失败 terminal、唯一 consumer 或跨 lifecycle reconciliation/isolation | 补三类 migration terminal、partial publication、recovery not-found/multiple/stale，以及 suspended work 的同 scope reconciliation/异 scope isolation | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-018` | blocking P2 | 第 5.2 节重复定义功能行为，NFR-010 owner 引用不完整，workflow switch 只存在于 NFR | 将 5.2 收敛为 evidence 判读；修正 base/provider owner；由场景、功能需求和 fixture 承接 workflow switch | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-019` | blocking P2 | 状态图先产生 `workspace_ready`，再判定 suspended work 的 scope/归属/隔离，允许标准资源在 exact plan confirmation 前创建 | 将 scope resolution 与 `suspended_work_plan_confirmed` 前置到任何标准资源副作用；资源准备后再验证 reconciliation/isolation，未收敛不得 Planning | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-020` | blocking P2 | clean install 被错误纳入 existing migration terminal；final-validation failure 又被固定解释为 cutover 后 | 为无旧 current 的 clean install 建立独立 application/validation route 与 `clean_install_blocked`；existing migration 的 application/final-validation finding 均按 live cutover state 分流 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-021` | blocking P2 | resume 将所有 stale/mismatch 无条件阻塞，与 equivalent identity freshness 合同及 NFR-009 的单一 current-phase 质量门槛冲突 | 区分 equivalent stale refresh/recovery 与 unresolved/material stale block；blocked 质量门槛改为 known candidates/facts、唯一所需输入和 resolution re-entry | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-022` | P3 | Evolution Design gate 的复审范围仍写 `REQ-REV-011..015`，漏列已进入 fresh review 的后续 finding | 将主文档与 task gate 范围始终统一为 current complete finding set；本轮为 `REQ-REV-011..024` | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-023` | blocking P2 | `REQ-UC-EVO-030` 的 completed archive history query 被压入 active-work recovery，唯一历史结果没有独立 success terminal | 区分 active resume/recovery 与 completed history query；新增 archived result current、query completed/blocked 和各自 resolution re-entry，已完成 task 不被重新激活 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-024` | blocking P2 | projection validation 只有成功路径，drift/sidecar/`.new/.bak` 等 mismatch 没有 owner、blocked result 或 exact re-entry | 由 `EVO-REQ-053` 定义 `projection_validation_blocked`、candidate/surface/mismatch 解释与修复后的 exact validation re-entry，并同步状态图、fixture 与 NFR 质量门槛 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-025` | blocking P2 | provider recovery 只显式覆盖 Delivery，没有约束 distribution、clean install、migration 与 Release 的外部动作 | 将 `EVO-REQ-039` 扩展为跨 route 外部 provider 合同，并由 `EVO-REQ-053..054,056`、fixture、NFR 与 return matrix 显式继承 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-026` | blocking P2 | existing migration 只证明切换合同，没有证明迁移前 active/resumable task 与 archived finish/history result 在新合同下仍可恢复/查询 | 在 cutover 前盘点并判定 preservation；不可承接则保留 pre-migration current 并停止，成功后经新合同恢复/查询且无 legacy runtime consumer | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-027` | blocking P2 | 并行 fixture 未覆盖 A=`github_pr`、B=`none` 混合 route 的 provider/archive/Finish/cleanup failure、双完成顺序和 retained-ref/history 隔离 | 扩展 `EVO-REQ-033`、`EVO-NFR-011`、`EVO-FIX-PARALLEL` 与 return matrix，证明两种顺序、失败归属、各自 recovery/history 与 cleanup reachability | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-028` | P3 | 第二章重复定义 `change_request/change_mode_selected` 的可执行选择规则，与第 3/6 章形成双主定义 | 第二章仅保留入口层级摘要与第 3/6 章链接，删除强制选择与 route 约束 | closed in prior Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-029` | blocking P2 | 当前候选曾使用晚于 `2026-08-24` 的 authority 日期，并把未发生的 fresh review 写成 ready/pass | 将日期恢复为真实证据日期，撤销 ready/pass，保持 draft 直到 exact candidate fresh 全稿复审 | closed in Requirements revision; fresh full review passed |
| `REQ-REV-030` | blocking P2 | live `.40` authority 已把 capability-loss 收窄为 `workflow/task_data/docs_authority`，但 Evolution trace 仍把 Skill API、distribution 与 installed consistency 混入 capability loss | 同步 `EVO-REQ-053`、NFR、fixture 与 `CUR-CAP-017..018`：两类 gate 都阻断，consistency/installation drift 不构成 capability loss | closed in Requirements revision; fresh full review passed |
| `REQ-REV-031` | blocking P2 | 顶层入口只有类别名，没有完整互斥分类；terminal history 后的新修改、distribution 合同修改、in-flight event 与 stop 的边界仍可能走错 route，且 stop 没有完整 terminal | 在 `EVO-REQ-010`、状态图、return matrix 与 fixture 中定义 new change/resume/distribution/specialist/stop 的互斥规则、歧义处理、in-flight 边界及 `request_stopped -> workflow_completed` | closed in Requirements revision; fresh full review passed |
| `REQ-REV-032` | blocking P2 | projection mismatch 一律由 projection owner 终止，未区分 standalone validation 与 clean install/migration/Release 内嵌 gate，允许 child validator 抢占 caller ownership | standalone mismatch 保留 `projection_validation_blocked`；内嵌 gate 只返回最小 finding，由 clean-install、migration、Release caller 分别产生 route-local blocked/terminal，并同步 NFR、fixture、状态/return matrix 与 inventory | closed in Requirements revision; fresh full review passed |
| `REQ-REV-033` | blocking P2 | current capability inventory 以 worktree 相对路径描述 `.40` authority，未把全套 Requirements/Design/Test/Architecture locator 绑定 immutable source ref，未 reconciliation checkout 可能被误读为 current | 在当时的 authority snapshot 上将 current authority 统一绑定 `source_ref=9c5fe383a4ec4d85b6ee0958419f9cc2b88b30c9`，逐项列出 Requirements main/NFR/decisions、Design inventory/main/traceability、Test strategy/traceability 与 Architecture README 的 immutable path；后续 live authority 漂移由 `REQ-REV-041` 处理 | closed in Requirements revision; fresh full review passed |
| `REQ-REV-034` | blocking P2 | additive intent 只有“并入未完成请求/返回最早 owner”的单一路线，未区分 owner-local 输入、改变 candidate/route/acceptance 的 material change，以及不可逆远端副作用开始后的 pending 新 invocation | 在 `REQ-UC-EVO-031`、`EVO-REQ-041,047`、状态回程、NFR、fixture 与 inventory 中定义三类 additive route；远端边界后的 material additive 必须形成不修改旧 candidate、可发现且有唯一 consumer 的 `additive_change_pending`，并在原 owner 收敛为既定正常 current terminal、失败后 forward-recovered terminal 或 terminal block 后，从新的 `request_received` 重新执行 entry selection | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-035` | blocking P2 | 不可逆远端副作用后的 generic override 在旧 route 收敛后被直接强制为 new change，绕过 `request_received -> entry_route_selected` 的五类互斥分类 | 在 `REQ-UC-EVO-031`、`EVO-REQ-047`、状态回程、NFR、fixture 与 inventory 中统一为：旧 route 收敛 current 后由唯一 request-entry consumer 生成新的 `request_received` 并重新执行五类 exactly-one entry selection，只有 new change 进入二级 mode selection，错误默认 new change 为 0 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-036` | blocking P2 | 不可逆远端副作用后的 material additive 虽重新执行 `entry_route_selected`，但被预设为 new change；distribution/release、resume/history、specialist review 或 stop 等正常追加意图会被误送到 change lifecycle | 与 generic override 使用相同的顶层分类合同：旧 route 三类结果任一 current 后，pending intent 从新的 `request_received` 重新执行五类 exactly-one entry selection；只有 new change 进入二级 mode selection 与独立 change lifecycle，其他分类返回对应顶层 route，错误默认 new change 为 0 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-037` | P3 | `requirement-main.md` 的 Design gate 仍停在 `REQ-REV-034`，且 `EVO-CAP-001/004` 未引用直接确认 generic override 修订的 `EVO-EVD-017` | 将 current finding/revision 范围同步到完整集合，并把 `EVO-EVD-017` 及本轮 material-additive 决定 `EVO-EVD-018` 加入两项核心能力 evidence refs | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-038` | blocking P2 | 本轮新增 revision metadata 与 `EVO-EVD-016..018` 使用晚于当前 live 日期 `2026-08-24` 的未来日期，重新引入 `REQ-REV-029` 已关闭的时间证据/authority 漂移 | 将 current Requirements/NFR/inventory 版本与最后修订日期、最新 EVD 日期统一恢复为 `2026-08-24`；保持 draft 门禁，重新执行 exact-candidate fresh 全稿审核，不声明 ready/pass | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-039` | blocking P2 | specialist wording 的 review-only 与 wording-as-change scope 未互斥；`pass`、`content_changed`、`blocked` 及 qualification 的非 classified 结果没有完整 owner、standalone terminal、fresh re-entry、fixture 与 NFR 闭环，可能把 wording 修订静默当成 pass 或丢给普通 Planning | 明确 review-only 只走 `specialist review`，文字本身属于交付范围时走 `new change`，new-change 内的 wording reviewer 只能是 route-local owner；补齐 wording/qualification 全部结果的唯一 caller、revision/recheck、scope/mechanism clarification blocked 投影、blocked/re-entry、active/standalone 消费，并同步状态图、return matrix、fixture、NFR、8.1/Goal/target/inventory；保持 draft 门禁，重新执行 exact-candidate fresh 全稿审核 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-040` | blocking P2 | current authority 绑定了本轮日期之后的 future ref，且 task-local candidate locator 把历史 baseline 标成当前 branch HEAD，可能让复核/恢复读取未来或错误 candidate | 在当时的 authority snapshot 上将 current trace 改绑到 `2026-08-24T23:12:31+08:00` 的 immutable `9c5fe383a4ec4d85b6ee0958419f9cc2b88b30c9`，明确排除当时的 `origin/main` future snapshot；把 `f962b640...` 改标为 planning baseline，并把带 future-dated Git metadata 的 `HEAD=2bd293d8...` 限定为 working-tree candidate locator，不作为 current authority/evidence，也不改写历史 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-041` | blocking P2 | `REQ-REV-040` 修订后 `origin/main` 已前进到包含 `.40` capability-loss 与 consistency/installation 语义更新的 `a4b68d42…`，但本候选仍把 current authority 绑定在旧的 `9c5fe383…`；Requirements、Design、Test 与 Architecture 因此可能从不同时间点读取 current，导致 `EVO-EVD-014/020` 已承接的新语义与 inventory/target trace 不是同一 authority snapshot | 将 Requirements current-authority 段落、inventory 的 `.40` authority locator 与 task authority record 全部重绑到已核实的 immutable `a4b68d42b25e3d2173fac2db353295043590cca5`，更新 snapshot/修订日期，重新验证 21 active Skills、89 external exits、六组 Test successor closure 与 `TARGET-DELTA-001..009` 映射；保留 `EVO-EVD-020` 为历史证据，不改写其用户确认，保持 draft 并重新执行 exact-candidate fresh 全稿审核 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-042` | blocking P2 | `docs/test/README.md` 将 `.40` `test-strategy.md` 与 `test-plan.md` 共同列为 current Test authority，但 inventory 只列 strategy/traceability，未明确 test-plan 是 authority locator 还是仅 evidence，可能让 current Test source 边界与六组 successor closure 被误读 | 增加同一 `a4b68d42b25e3d2173fac2db353295043590cca5` 下的 `test-plan.md` immutable locator；明确其承接执行计划、before-state、矩阵与 evidence boundary，不新增 `TST/SCN/CASE` source slice；重新验证 10 个 authority locator、六组 Test successor closure 与编号并集，保持 draft 并重新执行 exact-candidate fresh 全稿审核 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-043` | blocking P2 | 顶层 distribution/release 只选中大类，standalone projection、clean install、existing migration 与 exact Release 之间缺少基于 terminal intent 和 target live current state 的二级 exactly-one action，可能在验证、安装、迁移和发布之间误路由 | 在 `EVO-REQ-010`、状态图、return matrix、NFR、fixture 与 inventory 中定义二级四选一、歧义澄清、选择前零 route 副作用及内嵌 Release gate ownership；只有选中的 action 可继续 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-044` | blocking P2 | change-request readiness 只概括 duplicate/prerequisite/scope review，没有把 ready、active duplicate、completed duplicate、scope clarification、prerequisite blocked 与 resolution blocked 分为互斥结果及唯一 consumer/re-entry/terminal | 重写 `EVO-REQ-059`，为六类 readiness result 定义 exact owner、资源创建资格、completed terminal、clarification/prerequisite/resolution re-entry，并同步 fixture、NFR、状态/return matrix 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-045` | blocking P2 | standalone specialist review 报告 revision finding 后仍要求“修改或 stop”选择，导致只读 review 无法自然完成并可能静默扩张为写入请求 | 明确 standalone review-only 的 `pass` 与 revision findings 均在完整报告后直接进入 `standalone_specialist_completed -> workflow_completed`；后续修改必须作为新的 request，只有 blocked 保持 route-local re-entry | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-046` | blocking P2 | 单 task 在 Archive/Finish/Cleanup 部分完成或失败时只有泛化恢复语义，缺少分别可观察的 blocked 结果、已完成/待执行事实、live reread 与 exact re-entry，可能重放副作用或误报完成 | 在 `EVO-REQ-042` 中分别定义 `finish_blocked` 与 `cleanup_blocked`，绑定 exact task/resource/ref/history、已完成/待执行动作和唯一恢复输入；恢复后只重入对应 Finish 或 owned cleanup step，并新增 fixture/NFR/return-matrix/inventory 承接 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-047` | blocking P2 | Release publication 与 post-publish verification failure 只给出泛化 forward recovery/blocked，未区分 immutable publication blocked、可恢复 post-publish prerequisite failure 与已发布 candidate 的语义 defect，可能返回 pre-publish 或重铸原 tag | 在 `EVO-REQ-056` 中定义 `release_publication_blocked`、`release_post_publish_blocked` 与 terminal `release_published_unverified`；前两类只从原 immutable identity 的 exact owner 恢复；语义 defect 保留原 tag/Release，后续 request 先按统一 entry contract 重新分类，只有 `distribution/release` 的 exact-candidate Release action 创建新 candidate 并完整重跑 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-048` | blocking P2 | Release confirmation 把缺失、明确拒绝与 candidate/decision-relevant facts 变化导致的失效都保留在 `ready_for_release_confirmation`；明确拒绝没有 route-local terminal，stale wait 也可能继续消费旧确认进入 publication | 缺失确认仅在 candidate/pre-publish/关键 facts 全部 current 时保持等待；明确拒绝以零 publication 副作用得到 `release_not_published -> workflow_completed`；candidate/关键 facts 变化立即使旧 wait/确认失效并返回 Release pre-publish owner，candidate 变化生成新 identity 完整重跑，live facts 变化重读并重跑受影响 gate；同步 scenario、state graph、return matrix、fixture、NFR、Goal 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-049` | blocking P2 | current workflow authority 明确保留 simple conversation 与 non-file-changing request 的直接回答，但 target 顶层五类入口没有该 route；普通解释、事实/状态/信息查询可能被错误送入 change、specialist、distribution 或 lifecycle recovery | 在 `EVO-REQ-010` 增加 `direct answer`，定义与 history/specialist/change 的互斥边界、最小 live read、零 Issue/task/worktree/mode 副作用及 `direct_answer_completed -> workflow_completed`，并同步场景、状态、fixture、NFR 与 current capability trace | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-050` | blocking P2 | 取消、放弃或清理既有 active lifecycle 只能恢复 exact work 并交回 current owner，缺少 retain/suspend、confirmed abandonment cleanup、blocked re-entry、durable disposition history 与不可逆远端副作用后的 terminal 收敛 | 新增 `EVO-REQ-067` 和 `EVO-FIX-ACTIVE-DISPOSITION`：解析 exact current work 后按副作用边界形成 retain/suspend、确认后的 exact owned-resource abandonment cleanup、route-local blocked/re-entry，或保持 irreversible remote candidate 不变并收敛既定 terminal/forward recovery/terminal block；同步状态、return matrix、NFR 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-051` | blocking P2 | `specialist review` 顶层分类把所有“不修改对象的有界 semantic review”纳入，但正向需求、状态图、fixture 与 inventory 只支持 contract wording review-only 和 normal-scenario qualification；通用 Requirements/Design/代码/Architecture 只读 review 因而可能无 profile/owner/terminal，或与 direct answer 冲突 | 将 top-level specialist 明确收窄为 `REQ-UC-EVO-037..038` 两个受支持 profile；其它不绑定 active lifecycle、且不要求修改对象的通用只读分析/审核归入 direct answer，修改请求仍归 new change；同步 entry fixture、状态/return matrix、NFR 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-052` | blocking P2 | active lifecycle cleanup 明确拒绝后直接投影为 `retained \| suspended`，违反 `EVO-REQ-034` exactly-one result；同时 exact plan 中 deletable owned resource 为 0 时仍没有无副作用 abandonment terminal | cleanup 拒绝若已包含唯一 retain/suspend choice 则直接消费，否则只产生一次 `active_lifecycle_disposition_choice_required`；不得产生双终点。exact plan 证明 deletable resource 为 0 且保留项 current 时无需确认即可 no-op `active_lifecycle_abandoned`；同步状态、return matrix、fixture、NFR 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-053` | blocking P2 | abandonment 声明生成 durable disposition/history，但 history query 仅解析 archive/finish result；owned resource 删除后缺少 disposition result 的 discoverable index/query owner、success terminal 与 blocked re-entry 验收 | 将 history owner 明确为 archive/finish/disposition durable result 的唯一查询 owner；每个 disposition completion 必须在 cleanup 后仍可按 exact lifecycle/change identity 查询，统一得到 `durable_history_result_current` 或 `history_query_blocked -> durable_history_resolution`，且只读查询不得重入/激活 lifecycle；同步 fixture、NFR 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-054` | blocking P2 | direct answer 只有 completed terminal，但未定义所需 GitHub/provider/repository live facts 正常不可用、拒绝访问或响应不完整时的诚实结果；实现可能虚报 current，或为只读回答制造未定义 blocked lifecycle | 明确 direct answer 在所需 live fact 无法核实时返回具体 unavailable/unverified fact 与边界、禁止 current 断言，并仍以 `direct_answer_completed -> workflow_completed` 结束；后续重试是新 direct-answer invocation，不为读取失败创建 change/block lifecycle；同步场景、状态、fixture、NFR 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-055` | blocking P2 | 已确认 stock upstream Skill 会与 Guru Skill 共存并被自然语言自动匹配，但当前台账未逐项证明哪些必须由 Guru 替代、哪些只能作为 provider/explicit/controlled worker，也未闭合 patch/absence/quarantine/delete 的 provenance、用户修改保护、update/upgrade/reapply 恢复和 shared/host context parity | 在 `current-capability-inventory.md` 建立官方 `0.6.15` source binding、一次 shared layer 加三个 supported host 的 main/inline/sub-agent/channel/native context matrix、17 个 stock asset 的 exactly-one role/successor/caller/consumer/collision/action/fixture inventory，并新增 `EVO-FIX-STOCK-COEXISTENCE`、`EVO-FIX-STOCK-MAINTENANCE`；`requirement-main.md` 以 `EVO-REQ-068..074` 定义 suppression/provider/explicit-write/exception/provenance 合同，`requirement-non-functional.md` 以 `EVO-NFR-019..032` 定义门槛。用户已明确兼容性优先：完全替代且冲突的 asset 默认精确移除，只有保留不可替代能力时才增加 patch/quarantine/provider 复杂度；本轮不实施 upstream 修改、删除、提交或发布 | closed in Requirements revision; fresh exact-candidate review passed |

| `REQ-REV-056` | blocking P2 | README 已将 Shared 定义为 `.agents/skills` shared projection layer，但 inventory、fixture、NFR 与 task ledger 仍把 Shared 与 Codex/Claude/Cursor 并列为四个 supported projection；同时 Codex stock common/bundled Skill 的实际路径是 `.agents/skills/**`，`.codex/skills/**` 不是本版 stock projection，逐 cell closure 未明确 shared layer 与 host/context 的关系 | 统一为“一次 shared layer + Codex/Claude/Cursor 三个 supported host”；明确 shared layer 不重复计数，Codex 路径与 host agents/hooks 分离，host 的 main/default/inline/sub-agent/channel/native context 逐项闭合；同步 fixture、NFR、inventory completion contract 与 task gate，不把 source-only host 写成 coverage | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-057` | blocking P2 | 当前 `guru-clarify-requirements` 及其 contract 仍显式加载 `trellis-brainstorm`，但 inventory/target delta 已把它定义为 Guru authoring 必须完全替代的 `suppressed_semantic_route`；因此 target successor 与 current/target caller contract 直接矛盾，仍可能出现第二 questioning/authoring owner | 在 Requirements 中明确 current load 是待移除的 implementation fact；target `guru-clarify-requirements` 不得加载或依赖 `trellis-brainstorm`，questioning heuristics 必须由 Guru-owned source 承接；stock coexistence fixture 必须证明自然语言/显式匹配均 redirect/quarantine/fail closed 且无第二 question/approval | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-058` | blocking P2 | 当前 `guru-bootstrap-repository-ssot` 仍把 upstream `trellis-spec-bootstrap` 列在正向调用链中，但 inventory 只写 provider-only，未定义 raw semantic boundary 是否已移除以及失败时的唯一 owner；直接调用可能让 stock 自行选择 spec 边界并写 authority | 将 raw `trellis-spec-bootstrap` 明确改为 `suppressed_semantic_route`，由 Guru bootstrap 完全替代并抑制；任何未来 non-semantic adapter 必须使用独立 Guru identity，不能继承 raw projection/写入权；无法证明时返回 `upstream_suppression_blocked`，并补齐唯一 caller/consumer 与 fixture | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-059` | blocking P2 | `trellis-update-spec`、`trellis-meta` 与 `trellis-break-loop` 都可能真实写入 spec、local Trellis extension 或 follow-up，但 caller 仍写成模糊的 global stock-maintenance owner，未明确普通自然语言和显式调用边界 | 定义逐 asset 的 `STOCK-BIND-*` 与唯一 `stock-policy owner` binding；explicit-only 允许的写入必须绑定 exact action/target/caller/consumer，不能改写 repository RDT/Architecture/product/route authority；越界请求回到 Guru `new change` 或 fail closed，并补齐 provider boundary、retained consumer 与 Design handoff | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-060` | blocking P2 | stock suppression contract 已要求 template hash/manifest，但未把官方 package integrity、capture tarball identity 与目标 shared-layer/host projection cell 绑定到同一 provenance；仅凭版本字符串可能误把不同模板/平台输出当作同一 source | 在 `EVO-REQ-072`、`EVO-NFR-022..023` 与 inventory completion contract 中绑定 package name/version、registry integrity、capture tarball identity、source/path、projection cell、hash/sidecar 与 live file state；任何不匹配均保留现状并走 `upstream_suppression_blocked`/`provider_boundary_blocked`，修复后只重入 exact policy gate | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-061` | blocking P2（historical；role 计数已由 `REQ-REV-113..114` supersede） | 当轮候选中的 raw `trellis-spec-bootstrap`、explicit-only 写入能力、stock role 计数、九个 host-bound retained consumer 与 mutation-owner Design handoff 尚未在 task mapping 中形成同一闭环；该 finding 使用的历史 role 分类不得作为 current gate 输入 | 当轮已同步 `EVO-EVD-026/027`、`STOCK-BIND-*`/`STOCK-HANDOFF-*`、role/context counts、retained surface、handoff-vs-current-owner distinction 与 CLI/provider stimulus boundary；后续 `REQ-REV-113..114` 又把 raw before-dev/update-spec/meta 收敛为 suppressed，并以 current `9/1/2/5` role counts 取代本行历史计数 | historical finding closed; current role contract is defined by `REQ-REV-113..114` and current acceptance mapping; fresh exact-candidate review passed |
| `REQ-REV-062` | P3 | stock role completion contract 的表格行缺少 `Collision risk` 单元，无法逐行证明自然语言 collision coverage | 为 `trellis-spec-bootstrap` 行补齐 collision-risk 结论，并保持 17 个 logical asset 的列数一致 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-063` | P3 | `requirement-main.md` 第二章入口组织摘要遗漏 `REQ-UC-EVO-047..050`，stock policy/provider/maintenance boundary 在章节导航中不可追踪 | 增加非独立 semantic entry 的 stock policy/provider/maintenance 摘要并回指第 3/6 章主定义 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-064` | P3 | `EVO-FIX-STOCK-MAINTENANCE` 行中的竖线分隔符未转义，Markdown 表格产生额外列并破坏 fixture 语义 | 将 `pre_migration_current_preserved \| migration_blocked` 保持为单个转义分隔符，确保 fixture 表格列数一致 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-065` | P3 | task PRD 把当前 dirty candidate 描述为四个 Requirements/task-local 修订文件，实际包含本 task PRD 在内的五个文件 | 明确列出 README、主需求、非功能、current inventory 与 task PRD 五个文件 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-066` | blocking P2 | `EVO-REQ-070/072` 的 stock blocked exit 曾泛化为 suppression，与 NFR 的 role-specific provider boundary 规则冲突 | 按 role 分流：suppressed 使用 `upstream_suppression_blocked`；provider/explicit/worker 使用 `provider_boundary_blocked`，并同步状态回程、inventory 与 NFR | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-067` | blocking P2 | “普通自然语言不得自动触发任何 stock surface”未明确排除 Guru workflow 已绑定 caller 的内部 provider/worker 调用，可能把合法 `before-dev`/channel/worker provider 错当直接 matcher，或反向允许 stock 自行取得 semantic ownership | 明确禁止的是 stock matcher 的直接自然语言 dispatch；Guru workflow 只有在 caller、scope、source identity、consumer 已绑定后才能调用 provider/worker，且 provider 不取得 semantic ownership；同步 NFR auto-dispatch 口径并保持 draft | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-068` | blocking P2 | stock coexistence fixture 虽列出 Claude/Cursor 的 hooks-enabled 与 hooks-disabled/no-hook emission 差异，但没有要求两种配置都实际验证；仅测 hook-enabled 可能把 `trellis-start` 的过滤误当成完整 suppression | 将 Claude/Cursor `trellis-start` 的 hooks-enabled、hooks-disabled/no-hook 分支列为独立验收输入，并要求两者都在第一项 semantic 行为前 redirect/quarantine/fail closed；保持 draft | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-069` | blocking P2 | `EVO-REQ-072` 定义了 provenance 前置失败的 blocked 结果，但未定义 patch/absence/quarantine/delete 部分完成或 `unknown` 时的 per-action 状态、不可逆动作保护、live reread 与 exact owner re-entry；实现可能重放已完成动作、猜测未知结果或让后续风险动作继续执行 | 按 exact role/target/action 归类 completed/pending/unknown，绑定已完成/待执行动作、当前 file/sidecar state、不可逆边界和唯一恢复 owner；unknown 不得猜测成功，已完成/不可逆动作不得重放；恢复时复用仍 current 的 authority/policy context，仅在 package/source/projection identity、file/context/sidecar state 或 decision-relevant live facts 变化、过期或未知时定向 reread，再从对应 exact policy/caller owner 重入未完成或可安全重试 action，无法证明则保持同一 role-local blocked；同步 `EVO-NFR-022..023`、stock maintenance fixture、inventory completion contract 与 gate，保持 draft | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-070` | blocking P2 | `stock_policy_action_partial`/`unknown` 只存在于局部 fixture/NFR prose，Chapter 6 state graph 与 return matrix 没有 primary state、优先级、exact action owner 或 re-entry；部分完成可能被当作 current，未知结果可能重放已完成/不可逆动作 | 在 Chapter 6 建立 `stock_policy_evaluation -> stock_policy_action_partial \| stock_policy_action_unknown -> stock_policy_action_reentry` 主状态与 role-local return；`unknown` 优先，二者都不是 terminal，复用仍 current context，仅在 identity/state/decision-relevant facts 变化、过期或未知时定向 fresh reread，再只重入 pending/safe action，最终仅可到 `stock_policy_current`、对应 blocked 或 `retained_context_blocked`；同步 fixture/NFR/inventory，保持 Requirements draft | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-071` | blocking P2 | `EVO-NFR-023` 的“明确的用户保留状态”没有定义 state、owner、terminal 或 re-entry，且与 `EVO-REQ-072` 的用户修改/删除 role-local blocked 口径不一致 | 删除未定义结果，明确 `stock_policy_evaluation` 的 partial/unknown recovery-required 中间态和 suppressed/provider/retained 三类 role-local blocked；clean-install/migration/Release 仍由各自 caller 投影，补齐 `retained_context_blocked` 的 host owner/re-entry | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-072` | blocking P2 | `EVO-REQ-072` 的 action 词汇只覆盖 patch/absence/quarantine/delete，无法约束 provider/explicit/worker adapter/guard 或九个 retained host row 的 partial/unknown/recovery，导致 action state 可能被漏记或错误投影 | 在 `EVO-REQ-072` 建立 role-wide action family（suppression、provider/worker binding/guard、explicit binding/target guard、retained context preserve/reconcile、shared sidecar reconcile），并要求 17 个 role row 与九个 retained row 逐 action 覆盖 completed/pending/unknown、唯一 owner 和 `retained_context_blocked`；同步 fixture/NFR/inventory completion contract，不预选 Design 载体 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-073` | blocking P2 | `REQ-UC-EVO-048` 的 standalone explicit provider 结果没有明确落点；只读 provider 可能与 direct answer 混淆，窄维护写入也可能绕过 new-change/active-caller owner 或形成未定义 terminal | 明确只读 explicit provider 归 direct answer，先形成 `explicit_provider_result_current`（可携带 unavailable/unverified）再 `direct_answer_completed -> workflow_completed`；窄维护写入重新归 `new change` 或返回 active caller，embedded result 只走 `returned_to_unique_caller`，binding 不完整为 `provider_boundary_blocked`；同步状态图、return matrix、fixture、NFR 与 inventory | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-074` | blocking P2 | stock policy、worker/provider 与 maintenance 条款没有把真实 caller、profile、typed result/exit 和唯一 consumer 绑定到可观察 current contract；`guru-phase2-implementation-coordinator` 是 workflow/schema 引用但 active package 中不存在的 drift | 为每个 provider/worker/explicit row 增加 current caller evidence、input profile、最小 result/typed exit、consumer、回程和 `current_drift`/`design_handoff` 状态；`trellis-before-dev` 与 platform `trellis-implement` 必须分别覆盖 task-free implementation 与标准 Phase 2 implementation caller；标准 Phase 2 当前缺少真实 coordinator 时只能标为 `current_drift`/`design_handoff`；`STOCK-CONSUMER-*` 只能作为 handoff label，不能计为 closure；Requirements 不伪造不存在的 caller | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-075` | blocking P2 | 首次全 pending action 没有独立状态，evaluation fallback 可直接落到 `stock_policy_current`，无 action owner 或 re-entry | 增加 `stock_policy_action_required`，定义与 partial/unknown 的优先级、独立 `file_state`/`context_state`/`sidecar_state`、唯一 owner、必要确认和 `stock_policy_action_reentry`；同步状态图、return matrix、fixture、NFR 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-076` | blocking P2 | 显式 slash command、直接 Skill selection、upstream CLI、内部 caller-bound worker/provider、worker return/context 与普通自然语言未独立建模；句中提及 stock 名称可能和 explicit route 双匹配，且原优先级未覆盖 upstream CLI/provider | 建立 lifecycle preclassification 与六类 source stimulus/matcher matrix：active user intent 先按 bound/independent/ambiguous 和 owner-local/material/override/independent 结果处理，只有独立请求进入 `return/context > caller-bound provider/worker > upstream CLI > exact command > exact Skill selection > ordinary natural language`；同一 canonical identity 才允许 command/selection 折叠，否则 unknown/multiple fail closed；只允许 exact command/selection 构成 explicit，internal return/context 不重新顶层分流，并同步 fixture/NFR/return matrix | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-077` | blocking P2 | 官方 update 分支只写 generic update，遗漏 dry-run 的 `MIGRATION REQUIRED` discriminator、两条 exact command 和 `--skip-all` 用户修改保留语义 | 在 `EVO-REQ-077`、stock maintenance fixture、NFR 与 inventory 中要求：包含时 `trellis update --migrate --skip-all`，否则 `trellis update --skip-all`，两条都验证 user modification preservation；同一 invocation 复用仍 current 的 update policy context，仅在 `.new`/backup/sidecar 或 decision-relevant observed state 变化、过期或未知时定向 reread；保持为 upstream provider facts | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-078` | blocking P2 | 所有请求先进入完整 `stock_policy_evaluation`，direct answer/stop/history/specialist 承担不必要 inventory/provenance 成本并与其 route 条款冲突 | 将所有请求前置改为最小 `pre_semantic_dispatch_guard`；无 stock surface 的 direct answer、非 stock stop/history/specialist 跳过完整 policy，只有 stock-touching route/explicit/maintenance/provider 才进入完整评估；同步状态图、fixture、NFR | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-079` | blocking P2 | `STOCK-CONSUMER-*` 只是 handoff/result placeholder，不是真实 active caller/consumer；worker/provider 只有 Skill 名称，缺 profile、caller、worker/channel distinction、minimal result projection 和 exact return | 明确 placeholder 不计 closure，增加 §1.3.1 caller/profile/result/consumer matrix，区分 platform agent、channel runtime、spawning caller，并把不存在的 coordinator 标为 current drift/design handoff；同步 acceptance mapping 与 fresh gate | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-080` | blocking P2 | Codex hooks 只有单一 native cell，缺 hooks-enabled、hooks-disabled、no-hook 三种配置；Claude/Cursor start 的 emission 差异也未逐项验收，absence 可能被误当 suppression | 增加 Codex/host hook configuration matrix，三种输入分别验证 emission/context/suppression，明确 absence/not-emitted 不等于 suppression；同步 fixture、NFR、inventory 与 acceptance mapping | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-081` | blocking P2 | source stimulus 第一优先级把孤立 hook/context event 与 active worker return 混在一起；被动 startup/session/native context 可能伴随普通自然语言请求而抢占 Guru entry，且缺少 passive-context 负例 | 仅当 return/context 与 active invocation 因果绑定并同时具备 invocation/worker identity、caller、scope、source identity、profile、consumer 时才进入 rank 1；被动 context 只能作为 retained nonsemantic 输入，不消费请求、不改变 route；同步 `EVO-REQ-076`、source matrix、`EVO-NFR-028/032`、stock coexistence fixture 与 inventory | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-082` | blocking P2 | stock suppression 条款要求逐 asset 穷举 patch/absence/quarantine/delete 和反复 provenance reread，与用户要求的顺畅、快速、极简交接和最小上下文目标冲突，也可能让 stock policy 成为第二套 RDT/Architecture 读取链 | 以兼容性优先选择单一最小 action：语义重叠且已有 successor 时优先精确移除；仅对保留不可替代能力的 asset 选择 provider/explicit/patch/quarantine。未选 action 不要求额外验证；同一 invocation 复用一次 `authority_context`（RDT + Architecture + task delta），仅在 identity/决策相关事实变化时定向重读；同步 `EVO-REQ-026/071/072/078`、`EVO-NFR-022/023/032`、inventory target delta 与两个 stock fixture | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-083` | blocking P2 | `authority_context` 曾写成每个 invocation/阶段都绑定 RDT 与 Architecture，和 direct answer、非 stock stop/history/specialist 的最小 live read 约束存在歧义；若按字面实现会造成无关全文回读和重复 authority resolution | 明确 invocation-scoped、只建立一次的 context；先做最小 applicability 判断，标准 change/RDT-Architecture lifecycle/stock-touching route 才绑定适用 authority slices，direct answer 等只绑定答案所需 facts；同一 invocation 的 recovery/re-entry 复用，只有语义依赖变化才从最早 owner 定向失效重读；同步 `EVO-REQ-026`、`EVO-NFR-032`、inventory completion contract 与 stock fixture | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-084` | P1 | 外部 provider 失败与恢复已有 route/unknown/幂等原则，但未把 Git、GitHub、Trellis 三类 provider 的 source owner/contract locator、输入输出语义、生效时点、权限/错误/部分成功、重复请求和有限停止边界收敛为可验收合同，严格技术审核无法判定跨系统契约是否完整 | 在 `EVO-REQ-039` 增加 provider contract 最小维度和三类 provider 的边界字段；要求有限等待/尝试策略、live reread、权限失败零副作用，并在 `EVO-FIX-PROVIDER-RECOVERY` 绑定 exact source/target/result；不新增 Guru API/CLI intent，也不预选 adapter 实现 | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-085` | blocking P2 | `EVO-NFR-010` 只要求 provider/base 结果“可解释”，把日志/指标/public result 载体留给 Design，未规定 blocked/wait/partial/unknown 的最小可查询字段、唯一查询入口和事件触发边界，排障与验收可能只能依赖实现猜测 | 在 `EVO-NFR-010` 定义 owning caller/route（history 由 history owner）提供的最小 current result 字段、唯一查询入口、RDT/Architecture 适用片段和 provider identity；对 timeout/auth/quota/unknown/partial/stale/invalid handle/重复副作用/未消费 intent/orphan artifact 规定下一动作前的事件与既定 blocked/recovery 边界，同时明确不引入耗时/rounds/bytes 门槛；同步 provider fixture、权限边界和 `EVO-NFR-017` | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-086` | blocking P2 | `active_user_intent` 已被加入 source 讨论，但 lifecycle-bound 用户消息、独立新请求和普通自然语言之间仍缺少统一、可观察的 preclassification predicate；仅靠 source rank 可能重复生成 `request_received`、吞掉独立请求或在 active lifecycle 中误触 stock matcher | 在 `EVO-REQ-010,076`、状态图、latest-intent return matrix、`EVO-NFR-009,028`、`EVO-FIX-LATEST-INTENT` 与 inventory completion contract 中统一要求 exact invocation/lifecycle identity、唯一 owner/phase、scope/candidate/remote-boundary、host user-event identity、到达序列和 causal continuation facts；bound 消息只分类 owner-local/material/override/independent，predicate 不完整得到 `lifecycle_intent_binding_blocked`，独立请求建立新 envelope 并只生成一次新 `request_received` | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-087` | blocking P2 | stock admission 观测与实际 mutation action 仍在 source matrix、coexistence fixture 和 acceptance mapping 中混写；实现可能在 pre-entry guard 内执行精确移除、quarantine、patch 或 delete，造成未选 route 的副作用和重复维护 owner | 明确 `pre_semantic_dispatch_guard` 只可 redirect 或 role-local fail closed；精确移除/absence/quarantine/patch/delete 只能由已选且绑定 caller/scope/target 的 maintenance/provider action 承接，并由 `EVO-FIX-STOCK-MAINTENANCE` 单独验证；同步 `EVO-REQ-076,080`、NFR、inventory 与 return matrix | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-088` | blocking P2 | authority、stock policy/provenance 与 provider facts 虽已分别出现，但缺少明确的同一 invocation envelope / applicability projection 约束，可能重新形成多条完整回读链并增加 handoff/context 成本 | 在 `EVO-REQ-026`、`EVO-NFR-022,032`、inventory completion contract 与 stock fixtures 中要求一次 private invocation-scoped `context_envelope`，按需投影 `authority_context`、`stock_policy_context`（含 provenance）和 `provider_context`；无语义依赖变化只复用，变化时从最早受影响 owner 定向失效/重读，不新增 public schema | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-089` | blocking P2 | source-stimulus matrix 与 latest-intent fixture 没有同时覆盖 active user intent 的到达序列、owner-local/material/override/independent 四类结果和旧 route 收敛后的唯一新入口，可能遗漏、重复消费或把 pending intent 默认成 new change | 在 `EVO-REQ-041,047,076`、状态图、return matrix、`EVO-FIX-LATEST-INTENT`、`EVO-NFR-009,028` 与 `TARGET-DELTA-005` 中成对验证 bound/independent/ambiguous 输入、remote 前后 additive、override 三类副作用边界、唯一 consumer、旧 route 三类收敛和六类顶层重分类；保持 `requirements_ready_for_design=false` 直到 fresh review 通过 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-090` | blocking P2 | 状态图在 `lifecycle_bound_user_intent` 下再次产生 `independent_user_request`，与 `EVO-REQ-076` 的恰好三类 preclassification 及 bound 只能进入 owner-local/material/override 的合同冲突，可能让实现者重复生成 `request_received` 或绕过唯一 preclassification consumer | 删除 bound 分支下的独立请求 producer，统一由 preclassification 的唯一 `independent_user_request` 分支处理：无 active lifecycle 时立即生成一次新 `request_received`，已有 active lifecycle 时仅在旧 route current/forward-recovered current/terminal block 后生成；同步状态图、NFR 与 return matrix，保持 `requirements_ready_for_design=false` 直到 fresh review 通过 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-091` | P3 | `EVO-REQ-002` 的 Intake 与 `EVO-REQ-060` 的 pre-task 文字可能被实现为所有入口的全文 RDT/base 前置步骤；NFR-009 也未直说无 active lifecycle 时 independent request 可立即生成，和最小 applicability/read-once 目标存在表达歧义 | 将 Intake 限定为 `new change -> standard change` 的 requirement-authoring/readiness 步骤，将 pre-task 限定为 standard change 或明确依赖 selected base 的 route-local caller，并明确 direct/task-free/resume/history/specialist/stop 等非适用 route 只读取最小 facts；NFR-009 明确无 active lifecycle 时由唯一 consumer 立即生成新 `request_received`；保持 `requirements_ready_for_design=false` 直到 fresh review 通过 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-092` | blocking P2 | `independent_user_request` 在 active lifecycle 存在时仍被要求等待旧 route current/forward-recovered/terminal block；Release confirmation 等非变更等待可无限持续，与“无请求饥饿”质量目标冲突，且 pending 没有独立 owner/re-entry 语义 | 独立请求立即建立新 envelope 并生成一次 `request_received`；仅在共享可变 scope/不可逆动作尚未安全隔离时进入可发现的 `independent_request_isolation_pending`，绑定唯一 consumer、scope/owner 与 re-entry；非变更等待不得阻塞，安全边界或旧 owner terminal 后 exact re-entry，无法解析则 `independent_request_isolation_blocked`；同步状态图、回程矩阵、NFR、fixture 与 inventory | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-093` | blocking P2 | 无 active lifecycle 的 independent request 虽在功能/NFR 中定义为立即生成，但 `EVO-FIX-LATEST-INTENT` 只在 active fixture 内成对覆盖，`EVO-FIX-ENTRY-ROUTING` 与 inventory 没有显式输入/结果行，验收差集无法证明唯一 `request_received` | 在 latest-intent、entry-routing 与 `TARGET-DELTA-005`/inventory completion contract 中加入 no-active、active-wait、shared-scope-pending、bound 与 ambiguous 的独立输入及预期结果，要求无 active 分支立即生成一次新入口 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-094` | P3 | standard/task-free applicability 只以“file-changing request”措辞出现，未明确非文件变更请求不得进入 `change_mode_selected`，目标/范围不清时也缺少唯一终止/澄清边界 | 明确 `new change` 必须有文件变更目标；非文件请求按其它 top-level route 处理，确需文件变更但目标不清只问一次 scope clarification，choice 前无 mode/task/workspace 副作用；同步 entry fixture、return matrix、NFR-009 与 target trace | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-095` | blocking P2 | NFR-022 在质量门槛中再次规定“精确移除优先、仅保留不可替代能力才 patch/quarantine/provider”的 stock action 选择顺序，与 `EVO-REQ-071` 双主定义，可能导致 action 选择漂移 | 将 action 选择/兼容性优先级完全归 `EVO-REQ-071`；NFR-022 只校验已选 action 的唯一 identity、provenance、state、blocked/re-entry 与无重复读取，inventory 只引用主需求不另立选择规则 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-096` | blocking P2 | 四项正式核心能力的 `capability_id` 与 `capability_name` 只出现在章节标题，未作为表格字段，无法按标准包 5.2 做机器/审计字段闭包 | 在 `requirement-main.md` 四张正式能力表中补齐显式 `capability_id`、`capability_name` 行，并保持标题、候选池、来源/证据引用一致 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-097` | blocking P2 | specialist 状态图把 wording/qualification 结果无条件流向 `standalone_specialist_completed`，与 active caller 必须回自身 route 的正文和 return matrix 冲突 | 在状态图中为 pass/revision/classified 显式分出 active caller -> `caller_current_route_reentered` 与 standalone caller -> `standalone_specialist_completed -> workflow_completed` 两个互斥分支 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-098` | blocking P2 | 远端不可逆副作用后的 material additive/override 只写“生成新的 `request_received`”，未明确新 invocation identity/envelope，可能复用旧 receipt/candidate | 要求唯一 request-entry consumer 在旧 route current 后先分配新的 invocation identity、建立新的 invocation-scoped `context_envelope`，再恰好生成一次新 receipt；同步功能主定义、状态图、NFR、fixture、inventory 与 return matrix | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-099` | blocking P2 | NFR-009/028/032 与 inventory 的 TARGET-DELTA-005/完成项 11 重复定义 preclassification、receipt/isolation、source rank 与 envelope 行为，形成可漂移的第二功能 SSOT | 将这些位置收敛为可测覆盖率、零错误计数和单向主定义引用；functional behavior 只由 `requirement-main.md` 承载，inventory 只追踪 closure | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-100` | blocking P2 | `lifecycle_bound_user_intent` 的 no-side-effect/reversible-local override 分支曾直接生成 `request_received`，与 bound 事件不得自行产生 receipt、且新 route 必须拥有全新 invocation identity/envelope 的统一合同冲突；状态图、latest-intent fixture 与 return matrix 也未闭合该 identity boundary | 统一三类 bound override：bound 事件只由旧/current owner 消费并停止、挂起或收敛，不执行新 receipt 或再次 preclassification；旧 route 达到安全 current/forward-recovered current/terminal block 后，由唯一 request-entry consumer 分配新 invocation identity、建立新 invocation-scoped `context_envelope`，恰好生成一次 `request_received` 并重新执行顶层入口选择；新 invocation 不复用旧 identity、envelope、candidate 或 receipt；同步功能主定义、状态图、NFR、fixture、inventory 与 return matrix | closed in Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-101` | blocking P2 | 第 6 章曾声称拥有全部 conversational route/re-entry/terminal 产品语义，与第 3 章 `EVO-REQ-*` 功能主定义形成双主定义风险；NFR 与 inventory 的 no-active/release 摘要仍可能被读成绕过统一入口合同 | 明确第 3 章独占功能行为与产品语义，第 6 章只提供状态/回程投影；在 `EVO-NFR-009` 显式覆盖无 active lifecycle 的 independent input，并让 inventory 的 release 修订摘要只引用 `requirement-main.md` 的 `independent_invocation_entry_contract` | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-102` | P3 | 第二章 new-change 摘要仍把 standard/task-free 二级 mode 指向第 6 章，根 Requirements README 漏列 Evolution current-capability inventory，且 source-stimulus 的“每条用户消息”表述没有显式排除已绑定 provider/worker、upstream CLI 与 native context 作为新的顶层 preclassification 输入 | 将第 3 章标为功能与二级 mode 主定义、第 6 章标为状态/回程投影；把 `current-capability-inventory.md` 加入根 README 的 target 读取顺序；明确内部/上游已绑定 event 只在既有 invocation/caller 内按 source matrix 处理，不自行生成 `request_received` 或 top-level route | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-103` | blocking P2 | `EVO-REQ-056` 仍把 post-publish semantic defect 的每个后续修订强制分类为 `new change` 并立即产生新 candidate，与统一 entry contract、Release fixture、return matrix 和 inventory 已定义的“重新分类；仅 `distribution/release` 的 exact-candidate Release action 创建 candidate”不一致 | 由统一 entry contract 重新分类后，产品/合同/文档/代码修改先进入独立 `new change` lifecycle；其后只有新的 `distribution/release` exact-candidate Release action 才创建新 candidate并重跑 Release；其它分类不创建 candidate；同步状态图、fixture、目标总结、evidence 与 fresh-review 范围 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-104` | P3 | `requirement-main.md` 需求证据表新增行与既有表头列数不一致，结构化解析无法稳定判定 Evidence 记录边界 | 将全部 `EVO-EVD` 行规范化为同一三列表格，并重新执行 Markdown 解析与表格宽度闭包；不改变证据语义或 Requirements gate | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-105` | blocking P2 | isolation pending 已在功能主定义、状态图、回程矩阵和 fixture 中区分 progress/clear/blocked，但 NFR-009、并行 NFR-011 与 current inventory 仍只检查 pending/consumer，不能判定无进展、失效 handle 或不可消费 owner 是否会无限等待，独立请求仍可能饥饿 | 在 `EVO-NFR-009,011`、`EVO-FIX-ENTRY-ROUTING`、`EVO-FIX-LATEST-INTENT`、`EVO-FIX-PARALLEL` 与 inventory `TARGET-DELTA-005`/completion contract 中加入 progress-refresh、safe-clear/re-entry、no-progress/invalid-handle/non-consumable-owner -> `independent_request_isolation_blocked` 的逐分支覆盖与零无界 pending；不使用相对耗时/轮次阈值；保持 `requirements_ready_for_design=false` 直到 fresh review 通过 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-106` | blocking P2 | Requirements/quality contract 要求 workflow switch 先用 `--create-new` 预览并在字节、sidecar、用户修改检查后显式 `--force` 应用，但根安装 README 与 Guru workflow README 仍给出无 flag 的 active replacement，导致 provider stimulus 无法按需求重放 | 同步根安装 README 与 `trellis/workflows/guru-team/README.md` 到 preview -> preflight -> explicit `--force` application；明确 `--create-new` 不是 active replacement，保持 Requirements、quality guidelines 与现行入口文案一致；不改运行时实现 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-107` | blocking P2 | `trellis/presets/guru-team/README.md` 仍给出无 flag 的 active workflow replacement，与主 Requirements、quality guidelines 和其它入口的 preview -> preflight -> explicit `--force` 合同冲突 | 将 preset README 同步为 `--create-new` preview、字节/sidecar/user-edit/live identity preflight、同一 provider 的显式 `--force` application；明确无 flag replacement 不属于支持路径，不改运行时实现 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-108` | blocking P2 | migration matrix 的物理行序仍与 canonical provider 顺序不同，状态图把 cutover 前 application 直接落入 `migration_blocked`，return matrix 又把 final-validation finding 允许在 cutover 前分类，导致 composite terminal 与 live cutover 口径分叉 | 统一五个 substep 的物理/声明顺序为 `INSTALL -> UPGRADE -> UPDATE -> WORKFLOW-SWITCH -> PRESET-REAPPLY`；加入显式 `migration_cutover_current`；cutover 前 preflight/application 只返回 `pre_migration_current_preserved`，cutover/final-validation 后才允许 `new_contract_current \| migration_blocked`；composite 只做一次 final validation | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-109` | blocking P2 | distribution action 仍只以是否存在 Guru current workflow 区分 clean/migration，遗漏 workflow 缺失但存在 `.trellis/guru-team/`、managed projection、sidecar、lifecycle、history/ref 的 partial/legacy target；同时未明确 mixed/unknown/unowned/multiple identity 的零副作用阻断、Intake 的 Architecture applicability，以及五个 migration cell 的“按适用执行/全 N/A 仍 composite current”语义 | 在 `EVO-REQ-010` 建立唯一 `distribution_state_preclassification`：`clean_target`、`existing_migration_target`、`distribution_state_blocked`；同步状态图、return matrix、clean/migration fixture、NFR-013/023、inventory TARGET-DELTA-007 与 completion contract。将 `EVO-REQ-026` 明确拆为 Intake（Architecture=N/A）与 pre-design Planning（同一 envelope 按需消费 Architecture），并让 WORKFLOW-SWITCH 的 byte-equal observation 与 explicit `--force` 共同构成唯一 cutover；五个 supported cell 只执行适用 substep，全部 `not_applicable` 仍做一次 composite final validation | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-110` | blocking P2 | current authority 已前进到 `d907fcc5…`，但 inventory/task gate 仍把 `a4b68d42…` 当作 current，可能混读历史 snapshot 与新增 current capability | 将 Requirements current authority、inventory 全部 locator 与 task gate 统一重绑 `d907fcc5e17f23b6499648e5e9a208457f2d6f8b`；`a4b68d42…` 仅保留为历史 evidence，并重新核对 `.40` authority 内容、21 active Skills 与 89 exits | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-111` | blocking P2 | `82fb5172`/`f8d8b20d` 已为相关 Gitlink 增加 Phase 2 reviewed-content mode/pointer/content identity，但 current inventory 只到 `CUR-CAP-021`，target 可能丢失该能力或扩大无关 submodule scope/staging authority | 新增 `CUR-CAP-022` 并由 `EVO-REQ-028` 承接相关 Gitlink mode `160000`、pointer、初始化后的 root/clean/HEAD identity；保持无关 Gitlink 零读取/零验证及 Task Commit staging authority 分离 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-112` | P3 | `73973273` 已把 EOF-only 额外空行收敛为无严重度、非阻断 observation，但 Evolution current trace 未记录，可能恢复无价值 finding/revision loop | 新增 `CUR-CAP-023` 并由 `EVO-REQ-030` 承接 EOF-only meaningful-bytes-unchanged 例外，同时明确其它 whitespace 仍正常审核 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-113` | blocking P2 | raw `trellis-before-dev` 仍被 inventory/NFR/fixture 当成 provider，导致第二套 spec 全文读取链与 task-free/标准 Phase 2 caller 重叠 | 将 raw asset 改为 `suppressed_semantic_route`；由 `EVO-REQ-026` 的 Guru-owned `implementation_context` 从同一 envelope 薄投影承接两个 implementation profile，raw identity/auto-match/read chain 均不保留 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-114` | blocking P2 | raw `trellis-update-spec` 与 raw `trellis-meta` 仍被 inventory/NFR/fixture 当成 explicit-only，可绕过 RDT/Architecture governance、direct answer 与 canonical workflow/preset/overlay change lifecycle | 两项 raw asset 改为 `suppressed_semantic_route`；code-spec 能力由 governed contribution-to-projection 承接，Trellis reference 由 Guru lazy read-only projection 承接，写请求统一进入 Guru `new change`/active lifecycle；explicit-only 只保留 session-insight/break-loop | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-115` | blocking P2 | channel `implement` 仍固定指向 task-free owner，标准 Phase 2 无 profile/caller/consumer closure，与 platform implement 的双 profile 合同不一致 | 将 channel implement 绑定 task-free/标准 Phase 2 两个互斥 profile；每次 invocation 恰好选择一个 implementation owner、scope、typed result 与回程，缺少标准 caller 时保持 `current_drift`/`design_handoff` | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-116` | blocking P2 | current 正文仍把 provider/worker 泛称为可提供 memory/spec/reference，把 raw break-loop 写成可执行 spec/Issue follow-up，并在 explicit action/return matrix 中保留 raw explicit write result 与 reference/memory/transport/observation 混合 standalone 结果；这会重新授予已收窄 raw role 写入权或让 provider/worker 冒充 explicit/Guru successor | 按 `9/1/2/5` exact role 重构所有残留：channel 只提供 caller-bound transport；worker 只提供 caller-bound research/implementation/check result；session-insight/break-loop 只提供显式 history/只读 diagnosis/recommendation；spec/reference 只由 Guru successor 承接。删除 raw explicit write action，任何 follow-up 写入在 raw invocation 前回到 `new change`/active caller；同步 direct-answer、状态/return matrix、NFR、inventory 与 evidence | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-117` | blocking P2 | `distribution_state_preclassification` 把所有 non-Guru active workflow 固定留在 `foreign_workflow`，即使 identity/provenance/owner 与安全 transition plan 已 current，也没有正向进入 `existing_migration_target` 的 predicate；官方 workflow-switch 正常路径因此只能循环 blocked 或要求先 mutation 才能分类 | 将已识别 non-Guru workflow 的 current transition plan 作为 existing-migration 正向 predicate；未收敛时保持 `foreign_workflow -> distribution_state_blocked` 和零副作用，收敛后只重入 preclassification，source workflow 保留到 `MIG-CELL-WORKFLOW-SWITCH` 唯一 cutover；同步场景、功能主定义、migration matrix、状态/回程、fixture、NFR 与 inventory | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-118` | blocking P2 | hook 的 `hooks-enabled/hooks-disabled/no-hook` 高层配置与七个 setup discriminator 被写成近似交叉乘积，且没有 one-time approval surface 的 host 仍被要求覆盖 pending/denied，产生不可能 fixture、重复矩阵和与最小 applicability 相冲突的验收 | 将七个 setup cell 定义为 per-host、applicability-aware partition，高层配置从 cell 派生；每个 concrete fixture 恰好选择一个 cell，无 approval surface 时 pending/denied 由 host/provider fact 标为 N/A，enabled success 使用 `one_time_approval=not_applicable`；同步主需求、fixture、NFR 与 inventory completion contract | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-119` | P3 | `EVO-NFR-023` 的“每个 supported invocation 都经过同一个 guard”可能被理解为已绑定 migration/provider substep、worker return 或 upstream CLI event 重新执行顶层 admission，与 `EVO-REQ-076` 的 caller-bound reuse 边界不够一致 | 明确只有启动 top-level invocation 的独立用户输入执行一次 `pre_semantic_dispatch_guard`；已绑定 provider/worker/CLI/native event 复用同一 guard、envelope 与 caller-owned policy，不生成新 receipt、不重新顶层分流 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-120` | blocking P2 | `clean_target` 只排除了 Guru managed state，未排除已有 official Trellis config/template-hash/scripts/spec/task/workspace/platform footprint；当 active workflow 与 Guru projection 均缺失时会误入 clean route，而 `trellis init -y` 的 skip 写模式会保留旧 managed file，validation finding 后又禁止转入 migration，合法 existing repository 无法在原 route 收敛 | 将任何可唯一归属的 official Trellis/Guru managed installation state 排除出 `clean_target`；即使 workflow/Guru projection 缺失，也正向进入 `existing_migration_target`，由五-cell composite 承接 preserve-mode backfill、upgrade/update、workflow switch 与 preset reapply；unknown/unowned/mixed identity 继续 fail closed。同步场景、功能主定义、状态/回程、fixture、NFR、inventory 与 completion contract | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-121` | P3 | active workflow 在 invocation entry 已与 target 字节相等时，正文虽把 observation 排在 WORKFLOW-SWITCH，但“实际 live cutover state”仍可能被误读为从入口即 post-cutover，导致前序 INSTALL/UPGRADE/UPDATE finding 分类漂移 | 明确 byte-equal predicate 只能在固定顺序抵达 WORKFLOW-SWITCH boundary 后形成 cutover observation；invocation-entry byte equality 不跳过前序适用 substep、不提前推进 composite cutover，前序 finding 仍按 pre-cutover preservation 分类 | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-122` | blocking P2 | Chapter 3/migration matrix 已要求 cutover 后 mandatory `MIG-CELL-PRESET-REAPPLY` 再 final validation，但 Chapter 6 state graph 与 return matrix 从 `migration_cutover_current` 直接进入 `migration_final_validation_current`，状态投影允许跳过 mandatory step | 增加 `migration_preset_reapply_current`（适用 step current 或有依据的 `not_applicable` current observation）、role-local `migration_blocked` 与 exact re-entry，再进入 final validation；return matrix 显式列出 PRESET-REAPPLY finding | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-123` | blocking P2 | current inventory 的 explicit-only caller/result 表只给出 `explicit_provider_result_current`，却同时允许 standalone direct-answer 与 embedded/active caller 消费，并让 session-insight 可能被 durable history owner 接管，与功能主定义的互斥 result shape 冲突 | 按 asset 的 standalone/embedded profile 分行：standalone 使用 `explicit_provider_result_current -> direct-answer owner`，embedded 使用 `provider_result_current -> returned_to_unique_caller`；durable archive/Finish/disposition query 保持顶层 history/resume route ownership，logical role 计数仍为 `explicit=2` | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-124` | blocking P2 | `EVO-NFR-022` 把 suppressed raw successor 不可达称为 capability-loss blocked，与 capability-loss 只比较 `workflow/task_data/docs_authority`、其它 drift 归 consistency/installation 的主定义冲突 | 将其改为阻断性 capability-preservation/consistency finding；按 suppressed role 返回 `upstream_suppression_blocked`，明确不属于 capability-loss gate，也不得成为 `stock_policy_current` | closed in current Requirements revision; fresh exact-candidate review passed |
| `REQ-REV-125` | blocking P2 | 当前正常协作在已展示副作用计划、用户已回复“确认继续”后，仍要求固定 `确认执行 <hash>`；Evolution Requirements 只零散约束 confirmation 不持久化，没有独立规定语义等价肯定、计划完整性/实质变化、exact-action scope、READY PR merge 或禁止 identity challenge/script parsing，可能让 Design 继续把 digest challenge 当 freshness/授权 authority | 新增 `REQ-UC-EVO-051`、`EVO-REQ-081` 与 `EVO-FIX-SEMANTIC-CONFIRMATION`，同步 NFR-017、状态/回程矩阵、traceability、Design/Test/Architecture/task projection：完整 current 计划后的“确认继续”“可以，继续”等清晰肯定充分且只授权刚展示行动；READY PR merge 不要求 `合并PR`；未展示/疑问/限制/修改/拒绝/material drift 维持零副作用并重新展示或 route-local 收敛；固定 prompt/口令、hash/digest/task/path/branch/SHA/identity/摘要复述、规定句式、脚本解析及 authorization persistence 全部为 0 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-126` | blocking P2 | 新增 `REQ-UC-EVO-051` 后，阶段 0 候选池与 `EVO-CAP-001/EVO-CAP-004` 的 source/evidence/fixture 仍停留在旧范围，核心能力最终校正未承接 semantic confirmation 的 authority correctness 与 interaction-cost 难点 | 将 `REQ-UC-EVO-051`、`EVO-EVD-042`、`EVO-FIX-SEMANTIC-CONFIRMATION` 回写两个核心能力的候选、难点、展开粒度、成功指标、验证与来源证据，不新增第五个核心能力 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-127` | blocking P2 | `EVO-REQ-081` 与状态图已定义 explicit refusal，但回程矩阵只列疑问/限制/修改和 material drift，未给 refusal 的唯一 route-local terminal/re-entry | 在回程矩阵增加明确拒绝行：副作用为 0，只进入 exact action owner 已声明的 refusal/stop/re-entry，不复用或跨 action 扩张 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-128` | blocking P2 | Requirements 目标追踪仍称 Design/Test/Architecture mapping 尚未建立，但三类 candidate projection 已存在并已同步本轮 semantic-confirmation 合同；错误现状会诱发重复重建或让后续忽略 current draft projection | 明确 candidate mapping 已建立且仅为 `fresh_review_pending` planning projection；Requirements fresh gate 通过后必须重新绑定同一 exact ready identity，之后才可进入 Evolution Design 全稿审核 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-129` | blocking P2 | normal workflow 尚未把 repository RDT SSOT、Architecture Baseline SSOT 与 task `prd.md`/`design.md`/`implement.md` 明确定义为稳定、可缓存的主要上下文，也未明确 AI owner 应直接据此自主判断；Design 可能继续生成事务交接、重复事实摘要或把 cache/task artifact 升级为 authority | 同步 `EVO-REQ-044..046,049`、`EVO-NFR-001..008`、`EVO-CAP-002..004`、normal/full/long-output fixture、inventory completion、Design/Test/Architecture/task projection：稳定 locator/identity/order + 最小 live tail，cache 只作优化；human-style assignment/signoff/transaction handoff、already-current fact restatement、无 consumer 中间文档为 0 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-130` | P1 | 根 `docs/requirements/README.md` 仍声明 Evolution detailed Requirements 已通过 fresh 全稿门禁并处于 `requirements_ready_for_design`，与 target README、主定义、task PRD 和 trace projection 的 current draft 状态冲突，可能错误放行 Design | 将根 README 收敛为导航性 current draft 声明，明确历史 ready 结论因 material Requirements refinement 失效；不复制 gate 正文 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-131` | P3 | `MIG-CELL-WORKFLOW-SWITCH` 的 step phase 在 `shared composite` 前后交叉使用 inline-code delimiter，破坏表格可读性并可能干扰 Markdown parser | 将三个 phase token 分别放入独立 inline-code span，保持既有 cutover 语义不变 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-132` | blocking P2 | Requirements 主定义已明确 stable repository RDT -> Architecture Baseline -> task `prd.md`/`design.md`/`implement.md` context hierarchy、minimal live tail、cache 非 authority 与 AI owner 自主判断，但 Design/Test/Architecture/task projection 仍主要使用 generic envelope/reread 表述，未完整承接 cache unavailable 等价与零 human-style handoff/current-fact restatement，进入 Design 后可能再次丢失该合同 | 同步 `EVO-DES-011,067`、Test strategy、Plan/Full/Long-output fixture、Architecture concern/gap 与 task `design.md`/`implement.md`；只投影 stable locator/identity/order、最小变化 tail、cache 非正确性前提、AI 直接判断和零事务交接/事实复述，不新增 handoff/cache artifact 或开始 Design review | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-133` | blocking P2 | #311/#312 将先于 Evolution 完成，但 pre-`REQ-REV-133` candidate 仍绑定旧 base/能力清单并允许直接进入 Design/delivery，可能在重构中丢失 installed publication provenance source/target separation 或 original-worktree clean-tracked continuity | 新增 `REQ-UC-EVO-052`、`EVO-REQ-082`、`EVO-NFR-033`、`TARGET-DELTA-014` 与两项 fixture；扩展 `EVO-REQ-032/037/053` 和 base-evolution fixture；强制两项先 merge、exact base/RDT/Architecture/inventory rebind、全部能力 zero-loss comparison、merge-bound fresh Requirements/Design review，并把旧 Design/Test/Architecture/task projection 标为 stale；#312 折入既有 `CUR-CAP-012`，不新增核心能力或独立 delta，#311 的未来 owner 保持未选择 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-134` | P1 | `REQ-REV-133` 把 Design successor 零差集和 fresh Design review 同时写入 `requirements_ready_for_design`，但又禁止在该 Requirements gate 前修订 stale Design，重新引入 `REQ-REV-002` 已关闭的循环门禁；既无法合法进入 Design，也可能诱使实现绕过 Requirements gate 或把旧 Design 冒充 current | 分成两道不可互换的 gate：exact post-#311/#312 base rebind、current-capability requirement/normal-path fixture 零差集和 fresh Requirements review 只产生 `requirements_ready_for_design`；随后 fresh Design 才分配全部 Design successor/fixture mapping，fresh Design review 通过后才产生 `evolution_refactor_eligible`。同步主定义、NFR、状态/回程、fixture、traceability、inventory、Design/Architecture/task projection | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-135` | blocking P2 | #312 live regression contract 明确要求 source checkout 与 task worktree 的无关 dirty 状态不被误归类，但 current successor 只覆盖 clean-tracked 放行与 same-task/review/identity blocker，可能让 Evolution 用更宽的路径存在性检查重新阻断或错误触碰无关改动 | 在 `EVO-REQ-032,082`、`EVO-NFR-033`、核心能力指标、base fixture、inventory、trace 和 planning projection 中加入 unrelated-dirty isolation：保持原归属且不被 same-task 误分类、修改、stage、清理或忽略，同时不放宽真实 blocker | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-136` | blocking P2 | #311 live acceptance 要求 installed provenance reprepare 继续到唯一 Draft PR、archive/Ready 和 archive 后 `ready_for_merge` 且不重复 mutation，但 current fixture 只明确到 fast-forward publication identity，可能保住 metadata tail 却丢失完整 publication terminal continuity | 扩展 `EVO-REQ-037,053,082`、`EVO-NFR-033`、installed fixture、`TARGET-DELTA-014`、inventory、trace 和 planning projection：同一 reviewed-content/publication continuity 必须到唯一 Draft PR、summary、official archive 与 archive push、Ready、schema-valid `ready_for_merge`，terminal reinvoke 不重复 Git/GitHub/archive mutation；self-hosted control 和真实 blocker 保持 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-137` | P3 | merge 后 inventory 前半已把 #311 认定为 selected-base current capability，但后半仍把其 installed publication preservation 作为 `TARGET-DELTA-014`，并继续使用 `.40` Test closure；同一能力被同时分类为 current 与 new target，13/14 delta 计数和 successor comparison 自相矛盾 | 从 selected-base `.41` fresh 纳入 `REQ-047..051`、`TST-031..035`、`SCN-041..047`，把 #311 折入 `CUR-CAP-013/014/017/018/019`、#312 折入 `CUR-CAP-012`，删除重复 `TARGET-DELTA-014`，将 current coverage 与 completion contract 收敛为 23 current capabilities / 13 target deltas | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-138` | P3 | `.41` `REQ-051/TST-035/SCN-046` 要求 standalone verifier 在 cleanup 前保留 non-null structured failure evidence、matrix 外失败归类 `postcheck_failure` 且 Finalizer 零消费；current target 仅在 inventory 局部提及，缺少独立功能主定义、fixture、NFR 与全 projection 闭包 | 新增 `EVO-REQ-083` 与 `EVO-FIX-VERIFIER-FAILURE-EVIDENCE`，扩展 `EVO-NFR-010/016/033`，同步状态/回程、trace、Test、stale Design、Architecture contribution 与 task planning，形成 52 UC / 83 REQ / 33 NFR / 23 current capabilities / 13 target deltas / 50 fixtures | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-139` | P3 | live `origin/main` 已从 selected-base `3efcce72…` / active `.41` 推进到 `a41b8a34…` / active `.42`，而 Requirements、inventory、Design/Test/Architecture/task projection 仍把 `.41` 和旧 release mapping 当作 current；复用旧 gate 会违反 freshness，并可能把 `.42` 的 fact-only release-authority additions 错算成新 Evolution capability 或 target delta | 将 current source 统一重绑到 `origin/main@a41b8a34…`、`current-main-0.6.5-guru.42`、extension `0.6.15-guru.39`、target tag `v0.6.15-guru.3`；明确 `.42` 新增的 `REQ-052..055`、`TST-036..039`、`SCN-048` 只证明 authority/release facts，不新增 runtime behavior、`CUR-CAP-*`、`TARGET-DELTA-*` 或 Evolution fixture；首轮同步后的 fresh 全稿审核又发现三组同一 finding 的残余 stale-current：`requirement-main.md` 的 `.41` Design inventory 引用、`current-capability-inventory.md` 以 `.40` 为唯一排除基线的 `new_target` classification/introduction/completion 三项条款、`fixture-plan.md` 正例中的 `.41` rebind。三组均在 `REQ-REV-139` 下重闭合为 `.42` current locator、任意 selected-base current capability/PASS 排除规则与 `.42` rebind + `.41...42` fact-only promotion，并重新执行 fresh Requirements semantic、Strict technical、Design/Test/Architecture/task 与确定性闭包审核 | reclosed in current Requirements revision after residual stale-current correction; fresh full-document review passed |
| `REQ-REV-140` | blocking P2 | 前一 exact-candidate 全稿审核后，live `origin/main` 又从 `a41b8a34…` 推进到 `9f560ec1…`；虽然四个新增提交只修正 #267 post-merge lifecycle evidence、dogfood provenance、task archive 与 PR #316 merge facts，但 Requirements/inventory/navigation/Design/Test/Architecture/task projection 的 selected-base locator 和 fresh gate 仍绑定旧 ref，继续消费会违反 exact-authority freshness | 将 current source 统一重绑到 `origin/main@9f560ec1…`，把 `f5c47d28…`、`f7c5866e…`、`7d7db281…`、`9f560ec1…` 分类为 fact-only evidence delta；保持 active `.42`、extension `0.6.15-guru.39`、target `v0.6.15-guru.3`、CLI `0.6.15` 及 52 UC / 83 REQ / 33 NFR / 23 current capabilities / 13 target deltas / 50 fixtures / 73 Design responsibilities / 21 active Skills / 89 exits不变，不新增 runtime behavior，并重新执行 Requirements、Strict technical、Design/Test/Architecture/task 与确定性闭包审核 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-141` | blocking P2 | live `origin/main` 从 `9f560ec1…` 推进到 `736ef333…`；PR #317 修复 installed Finalizer reprepare 的 exact platform-selection preservation 并更正 latest stable current identity。该 delta 是 material current capability advance，旧 Requirements/Design gate 继续绑定 predecessor 会丢失单平台/子集保真、错误 `all_platforms` fail-closed 与 source-checkout/apply/commit 前零 mutation 合同 | 新增 `EVO-EVD-050`、`EVO-REQ-084` 与 `EVO-NFR-034`，扩展 `CUR-CAP-013/014/017` 和既有 installed-publication fixture；selected lists 非空/有序/唯一/canonical/三处一致，`all_platforms=true` 仅允许完整 canonical set 并映射 `--all-platforms`，其余 exact set 映射 ordered repeated `--platform`，invalid identity 在 source checkout/apply/commit 前 fail closed；同步 84/34 Requirements、Design/Test/Architecture/task projection，保持 52/23/13/50/73/21/89 集合不变并重新执行 fresh Requirements 双审 | closed in current Requirements revision; fresh full-document review passed |
| `REQ-REV-142` | blocking P2 | fresh Design 全稿审核期间，live `origin/main` 从候选绑定的 `736ef333…` 推进到 PR #318 merge `5650df47…`；即使 delta 仅修复 Finalizer caller-inventory anchor、Issue Scope Ledger、dogfood provenance 与 archive/merge facts，旧 exact-base binding 仍违反 current freshness 合同 | 新增 `EVO-EVD-051`，将 current source 统一重绑到 `origin/main@5650df47…`；把 `736ef333...5650df47` 分类为 fact-only inventory/provenance consistency advance，保持 `.42`、`.2/.38/CLI 0.6.15` current、`.3/.39` unverified 以及 52/84/34/23/13/50/73/39/43/37/21/89 集合和 #311/#312/#317 successor 不变，并重新执行 fresh Requirements、Architecture freshness、Design 全稿与确定性闭包审核 | closed in current Requirements revision; fresh full-document review passed |

### Current revision alignment

`REQ-REV-091..094` 的修订统一采用同一顺序：independent request 先建立新 invocation identity 与
`context_envelope`，恰好生成一次只表示 admission 的 `request_received`，再执行 isolation safety gate；
shared-scope/不可逆动作尚未安全隔离时才在 route selection 前进入
`independent_request_isolation_pending`，安全回程复用既有 receipt，不能重复 receipt；无法证明隔离时为
`independent_request_isolation_blocked` 并重入 gate。`REQ-REV-094` 另外明确只有真实文件变更
才进入 `change_mode_selected`；非文件请求留在其它顶层 route，文件变更目标不清时只执行一次
scope clarification，choice 前保持零 mode/task/workspace 副作用。`REQ-REV-095` 的 action 选择仍只由
`EVO-REQ-071` 主定义，inventory/NFR/PRD 只记录或校验已选 action，不另立选择顺序。
`REQ-REV-096..100` 继续遵守单一来源：核心能力字段由四张正式表显式承载，specialist caller
projection 由状态图/return matrix 互斥承接，远端 pending intent 先建立新 identity/envelope 再生成
receipt；`REQ-REV-100` 的 bound override 消费与 re-entry 由功能主定义、状态图和 return matrix
闭合，bound 事件本身不产生 receipt，旧 route 安全收敛后才建立新 identity/envelope 并生成一次新
receipt；`REQ-REV-101` 进一步明确第 3 章是功能行为 SSOT、第 6 章只是状态投影，并把 no-active
coverage 与 release inventory summary 收敛为对统一入口合同的单向引用；NFR/inventory 只保留质量
门槛和 closure 引用，不复用旧 candidate 或 envelope。`REQ-REV-102` 进一步把第二章入口摘要、根
Requirements README 的 target 读取顺序与 source-stimulus 的内部/上游 event 边界收敛为单向导航和
admission 语义，不新增 route 或 receipt 合同。`REQ-REV-103` 进一步把 post-publish semantic-defect
follow-up 收敛为统一 entry reclassification：new change 先完成独立 change lifecycle，只有后续
`distribution/release` exact-candidate Release action 才创建新 candidate；状态图、fixture、目标总结与
evidence 只引用该边界，不另立 route 或 candidate 合同。`REQ-REV-104` 只修正
Evidence 表的结构化列数，不改变证据语义。此说明是
task-local 修订索引，不替代 Requirements SSOT；该轮当时的 gate 保持
`requirements_draft` / `requirements_trace_draft`，直到 `REQ-REV-125..128` 的完整 projection 通过 fresh gate。
该条件曾在 `REQ-REV-125..128` 的 candidate 上满足；`EVO-EVD-043` 后的
`REQ-REV-129..132` 也曾通过修订后 exact candidate 的 fresh gate。`EVO-EVD-044` / `REQ-REV-133..136`
随后使两轮结果都成为历史 evidence；`REQ-REV-137..138` 完成 selected-base rebind/zero-loss 修订；
`REQ-REV-139` 又将同一 capability set 重绑到 `origin/main@a41b8a34…` / `.42`，并证明 `.42` 是
fact-only authority promotion；`REQ-REV-140` 再把 unchanged capability set 重绑到
`origin/main@9f560ec1…`，证明 `a41b8a34...9f560ec1` 只更新 lifecycle/provenance/archive/merge facts。
`REQ-REV-141` 随后把 current authority 重绑到 `origin/main@736ef333…`，将 PR #317 的 material
platform-selection advance 收敛为 `EVO-REQ-084` / `EVO-NFR-034` 与既有 fixture successor。该 revision
不改变 52 UC、23 current capabilities、13 target deltas、50 fixtures、73 Design responsibilities、21 active
Skills 或 89 exits，但使旧 Requirements/Design gate stale。`REQ-REV-142` 再把 current authority 重绑到
`origin/main@5650df47…`，证明 `736ef333...5650df47` 只更新 caller-inventory identity、Issue disposition、
dogfood provenance 与 task archive/merge facts，不改变任何 Evolution behavior/capability/requirement/fixture/
Design/Skill/exit 集合。current 状态只以本文件 `Current Phase Boundary`
中的 `requirements_ready_for_design` / `fresh_design_review_passed` / `evolution_refactor_eligible` 为准。

`REQ-REV-105` 进一步把 isolation pending 的 liveness 收敛为事件/状态进展合同：只有存在可观察
下一进展时才保持 pending；每次重评估必须 progress-refresh、safe-clear/re-entry 或 blocked，
无进展、失效 handle、不可消费 owner 与无限期 pending 均不得继续。NFR、并行 fixture、inventory 和
return matrix 只验证该分支覆盖与零错误计数，不复制功能主定义。

`REQ-REV-106` 将 workflow-switch 的 provider 文案投影与 Requirements/quality contract 对齐：
预览只产生 `.new`，必须先完成字节、sidecar、用户修改和 live identity preflight，再由同一
provider command 使用显式 `--force` 应用；无 flag replacement 不属于支持路径。该修订不改变
Guru runtime 行为或本阶段的 Design 边界。`REQ-REV-107` 将 preset README 的同一 provider
文案同步为 preview/preflight/explicit-force；`REQ-REV-108` 将 migration substep 的物理顺序、
显式 cutover phase 与前后 failure classification 收敛为 composite-only terminal 合同。

`REQ-REV-109` 进一步把 install/migration 的入口统一收敛到一次
`distribution_state_preclassification`：真正无任何可归属 official Trellis/Guru managed installation
state 的 target 才是 `clean_target`；可唯一归属的 official Trellis footprint 或 partial/legacy Guru
surface 即使缺少 current workflow/Guru projection 也进入
`existing_migration_target`；mixed/unknown/unowned/multiple identity 在 clarification/live repair
前保持 `distribution_state_blocked` 与零 route 副作用。Intake 的 Architecture slice 明确为
`N/A`；standard pre-design Planning 从同一 `context_envelope` 依次消费 RDT 与 Architecture projection，
direct answer、resume/history、specialist 与 stop 四类非适用 route 不创建这两个 section；
migration 五个 cell 逐一判定 applicability，只执行适用 substep，全部 `not_applicable` 仍形成
composite current 并执行一次 final validation。

`REQ-REV-110..115` 将 fresh `origin/main=d907fcc5…` 设为统一 current authority，保留
`a4b68d42…` 仅作历史 evidence，并把两项 post-`.40` current capability 写入
`CUR-CAP-022..023`。stock role 同步收敛为 `9/1/2/5`：raw before-dev/update-spec/meta 全部由
Guru successor 无损承接后 suppression，explicit-only 仅余 session-insight/break-loop；platform 与
channel implement 均按 task-free/标准 Phase 2 双 profile 绑定唯一 caller。该修订只改变
Requirements candidate，不实施 suppression、adapter、worker 或 Design。

`REQ-REV-116` 进一步消除 role-local 词汇残留：channel transport、worker result、explicit-only
history/只读 diagnosis 与 Guru-owned spec/reference successor 完全分离；raw break-loop 不再拥有
spec/Issue/文档/代码写入 action，写入意图在 invocation 前回统一 Guru route。该 current contract
取代 `REQ-REV-059/073` 历史行中曾允许 raw explicit narrow write 的旧模型；历史行只保留修订轨迹，
不再构成 current authority。

`REQ-REV-117..119` 进一步闭合 existing repository 的正向 workflow-switch 入口、hook setup 的
applicability-aware partition 与 single-guard reuse：已识别 non-Guru workflow 在 current transition
plan 收敛后进入 existing migration 且保留到唯一 cutover；高层 hook configuration 不与七个 setup
cell 交叉相乘，无 approval surface 的 pending/denied 为 N/A；已绑定 provider/worker/CLI event 不重跑
顶层 guard。三项仍只修订 Requirements，不执行 workflow mutation、fixture 或 Design。

`REQ-REV-120..121` 进一步闭合 official Trellis-only existing repository 与有序 byte-equal cutover
observation：任何可唯一归属的 official Trellis/Guru managed installation state 都不能进入 clean
fixture；workflow/Guru projection 缺失不改变 existing-migration 归属。byte equality 只有在固定顺序
抵达 WORKFLOW-SWITCH boundary 后才推进 composite cutover，invocation entry 的相同 bytes 不改变此前
适用 substep 的 preservation 分类。两项仍只修订 Requirements，不执行 provider mutation、fixture 或
Design。

`REQ-REV-122..124` 进一步闭合三项跨投影一致性：migration cutover 后必须显式抵达
`migration_preset_reapply_current` 并拥有自己的 blocked/exact re-entry，才可执行 final validation；
standalone 与 embedded explicit-only profile 使用互斥 result/consumer 形状，durable history 不接管
standalone provider terminal；suppressed successor 不可达属于 capability-preservation/consistency
finding，并固定返回 `upstream_suppression_blocked`，不再混入 capability-loss gate。三项仍只修订
Requirements，不执行 Design、fixture、provider mutation 或 implementation。

`REQ-REV-125` 将此前被局部 Skill 正确约束、但未在 Evolution Requirements 独立闭合的确认语义
提升为产品合同：AI 只根据 current 对话接受语义肯定，固定 prompt/口令/hash/digest/task/path/branch/
SHA/identity/摘要/规定句式 challenge 与确定性组件解析/持久化 confirmation 都被排除；旧确认在计划
不完整、回复修改或 material facts 变化时失效；
一次确认只消费刚展示 action，READY PR merge 使用同一规则。本轮同步 Design/Test/Architecture/task
projection 后重新执行 fresh gate，不产生 implementation 或 Git/GitHub 副作用。

`REQ-REV-129` 将 repository RDT、Architecture Baseline 与 task 三件套收敛为稳定的主要 context
hierarchy：task 三件套只承载 task-local delta/projection，live facts 位于最小变化 tail，cache 仅是不可依赖
优化；AI owner 直接据 current authority 自主判断，不消费 human-style assignment/signoff/transaction
handoff 或 already-current fact summary。该修订不新增 handoff/public DTO/cache artifact 或实现机制，
并使上一轮 Requirements/Design input binding stale，必须重新执行 fresh gate。

`REQ-REV-132` 关闭该原则在下游 planning projection 中的承接缺口：Design/Test/Architecture/task
projection 均显式使用同一 stable authority/task-doc prefix、minimal live tail、cache-unavailable
correctness equivalence 与 AI-owned direct judgment，并把 human-style handoff/current-fact restatement
计数固定为 0；该修订只同步 Phase 1 projection，不执行 Design review 或实现。

`REQ-REV-133..136` 又将 #311/#312 收敛为 Evolution 强前置和完整 merged-capability preservation gate：两项在
 selected base 可达以前，旧 Design/Test/Architecture/task projection 全部 stale；两项可达后仍必须先从
 exact base fresh rebind authority/inventory、比较全部 requirement/normal-path fixture 差集并重做 Requirements
 全稿审核，之后才进入 Design re-entry；Design successor 与 fresh Design review 再单独放行 refactor。
installed publication terminal continuity、clean-tracked/unrelated-dirty workspace behavior 与真实 blocker 均在
同一能力集内。`EVO-EVD-045` 随后记录了 selected-base live reconciliation：#311 为 `OPEN + follow-up-only`
而 #312 为 `CLOSED`，二者均已具备可达 merge identity；该修订不把 lifecycle 标签当作其它维度的替代，也不
预选 #311 的 Design/runtime owner。

`REQ-REV-137..138` 完成 merge 后 current-to-target 收敛：`.41` 的 #311 installed publication 与
verifier failure behavior 是 selected-base current capability，不是第 14 个 new target delta；#311 因此
折入 `CUR-CAP-013/014/017/018/019`，#312 折入 `CUR-CAP-012`。新增 `EVO-REQ-083` 与独立 verifier
failure fixture 后，Requirements-stage closure 为 52 UC / 83 REQ / 33 NFR / 23 current capability /
13 target delta / 50 fixture；Design 仍不获得任何新 owner/DTO/Skill allocation。

`REQ-REV-139` 处理后续 authority freshness：`.42` 取代 `.41` 成为唯一 active RDT/Architecture
knowledge identity，current source candidate/Release target 分别为 `0.6.15-guru.39` 与
`v0.6.15-guru.3`，但 tag、GitHub Release、tag-pinned smoke 与 #311 post-release proof 仍为
`unverified`。`.42` 的 `REQ-052..055`、`TST-036..039`、`SCN-048` 只描述该 fact-only promotion 与
release gate，不是新的 Evolution 产品能力，因此现有 23 current capabilities、13 target deltas 和 50
fixtures 原样保持。

`REQ-REV-140` 处理再一次 exact-base freshness：`9f560ec1…` 取代 `a41b8a34…` 成为 selected
`origin/main` locator；四提交 delta 只校正 #267 post-merge lifecycle evidence、dogfood source provenance、
task archive 与 PR #316 merge facts。它不改变 `.42` authority version、`.3/.39/CLI 0.6.15` mapping、
任何产品/runtime behavior 或现有 UC/REQ/NFR/capability/delta/fixture/Design/Skill/exit 集合。

`REQ-REV-141` 处理 PR #317 的 material current behavior advance：selected base 变为 `736ef333…`；parent
manifest 的 `install.selected_platforms`、`install.all_platforms`、`skill_packages.selected_platforms` 与
`overlays.selected_platforms` 形成 exact identity，合法单平台、subset、true-full 与 false-explicit-full
selection 均保持原集合，invalid identity 在 extension source checkout、preset apply 与 provenance commit 前
fail closed。该能力通过既有 `CUR-CAP-013/014/017` 和 installed-publication fixture 承接，增加第 84 项
requirement 与第 34 项 NFR，不增加 UC、核心能力、target delta、fixture、Design responsibility、Skill 或 exit。

`REQ-REV-142` 处理 PR #318 的 fact-only selected-base advance：current source 变为 `5650df47…`；
`5afe15a6…` / `1cd52901…` 只修正 Finalizer second-hop caller-inventory identity 与 #267 related-only
Issue Scope Ledger，`f6960c24…` / `d06da2ca…` / `5650df47…` 只更新 dogfood provenance、task archive 与
merge facts。Finalizer runtime、public Skill I/O/schema/typed exit、`.42` RDT/Architecture、Release identity、
52/84/34/23/13/50/73/39/43/37/21/89 集合和 #311/#312/#317 successor 均不改变。

## Evidence Mapping

| Evidence | Inherited review revisions | Live source / scope | What it proves | What it does not prove |
| --- | --- | --- | --- | --- |
| `EVO-EVD-026` | `REQ-REV-056`, `REQ-REV-060` | `@mindfoldhq/trellis@0.6.15` registry integrity `sha512-grbF8PToesHojsaWkoG4+Aupih7eZHkXH5y33uzPrWQXwIRewwlM1AoeJEttcXAia9nLZzF/ezuR338PWCKv+A==` and capture tarball SHA-256 `7b97e4247f54e71f22ff80caa328d9e68fb81908f984f15d70a4d81cc2a0306c` | Common/bundled/worker source slices, shared projection behavior and update sidecar facts used by the stock inventory; the two listed review revisions inherit this same package/projection provenance | No target suppression, adapter, delete, or Guru CLI contract is implemented by this evidence |
| `EVO-EVD-027` | `REQ-REV-057`, `REQ-REV-058`, `REQ-REV-059` | Frozen Guru workflow/preset ownership facts at `source_ref=a4b68d42b25e3d2173fac2db353295043590cca5` plus current canonical overlay paths | Current source owner is upstream, Guru policy/handoff is required, and missing mutation/interception owner must be represented as a blocked Design boundary; the three listed review revisions inherit this ownership/boundary evidence | No current mutation owner, patch, absence/quarantine, delete, or implementation owner/action is established |
| `EVO-EVD-028` | `REQ-REV-081..091` | `2026-08-25` user decision: the target flow must be smooth/fast, preserve all current capability, avoid repeated rereads, use minimal handoffs/context, and integrate deeply with repository RDT and Architecture Baseline SSOT; incompatible upstream skills may be modified or precisely deleted | Product-level direction for lifecycle/source-stimulus admission and passive-context isolation (`081`,`086`,`089`,`090`), minimal invocation-scoped authority/context reuse and route applicability (`082`,`083`,`088`,`091`), explicit Git/GitHub/Trellis provider contracts and observable recovery results (`084`,`085`), and the admission-versus-maintenance mutation boundary (`087`) | No upstream mutation/deletion, provider adapter, observability carrier, or Design implementation is established by this evidence; the decision only supplies product constraints and does not prove that either fixture has executed |
| `EVO-EVD-029` | `REQ-REV-092..095` | `2026-08-25` 本候选产品决定：独立请求不得因旧 lifecycle 的无界等待而饥饿；必须有无 active lifecycle 的显式验收输入；standard/task-free 只承接真实文件变更；stock action 选择规则保持单一主定义 | 本候选已按该决定完成局部 Requirements 修订并保持 draft gate；下游 Design、提交、发布和 provider/isolation implementation 不属于本证据范围 | 不证明实现、测试执行或 Requirements gate 通过 |
| `EVO-EVD-030` | `REQ-REV-096..100` | `2026-08-25` 本候选产品决定：正式能力表必须具备完整字段；specialist caller/standalone terminal、远端 pending intent identity/envelope、bound override re-entry identity/envelope 与 functional SSOT 必须单向收敛 | 本候选已按该决定完成局部 Requirements 修订并保持 draft gate；下游 Design、提交、发布和 provider/isolation implementation 不属于本证据范围 | 不证明实现、测试执行或 Requirements gate 通过 |
| `EVO-EVD-031` | `REQ-REV-102` | `2026-08-25` 本候选产品决定：入口摘要与 target 文档读取导航保持单一来源，source-stimulus 与已绑定内部/上游 event 的顶层 preclassification 边界明确 | 本候选已按该决定完成局部 Requirements 修订并保持 draft gate；下游 Design、提交、发布和 provider/isolation implementation 不属于本证据范围 | 不证明实现、测试执行或 Requirements gate 通过 |
| `EVO-EVD-032` | `REQ-REV-103` | `2026-08-25` 本候选产品决定：post-publish semantic-defect follow-up 必须按统一 entry contract 重新分类，new candidate 只由 `distribution/release` 的 exact-candidate Release action 产生 | 本候选已按该决定完成局部 Requirements 修订并保持 draft gate；后续请求沿用统一 entry contract；下游 Design、提交、发布和 provider/isolation implementation 不属于本证据范围 | 不证明实现、测试执行或 Requirements gate 通过 |
| `EVO-EVD-033` | `REQ-REV-104` | `2026-08-25` 确定性审计结果：`requirement-main.md` Evidence 表新增行与表头列数不一致；本候选已将全部 Evidence 行规范化为三列 | 证明表格结构可被统一解析；不改变任何 Evidence 语义、route、能力或 gate 结论，也不扩展本候选阶段范围 | 不证明实现、测试执行或 Requirements gate 通过 |
| `EVO-EVD-034` | `REQ-REV-106` | `2026-08-26` 对官方 `@mindfoldhq/trellis@0.6.15` CLI 与 canonical compatibility verifier 的 live 复核：CLI 的 `init --yes/--skip-existing` 在已有文件上采用 skip 写模式；但 existing verifier cell 的初始 setup 使用 before-source 的 `trellis init -y --workflow guru-team` 并安装旧 preset，随后按 `upgrade -> update(dry-run 分支) -> workflow preview/force switch -> preset reapply` 串行执行，整条链只做一次 composite validation，未执行 preservation-mode `trellis init -y --skip-existing` migration substep | 证明当前 verifier 的四阶段 provider chain 与一次 composite validation 的事实，并支持把 preservation-mode INSTALL 作为目标 migration substep/独立 fixture 纳入五-cell composite | 不证明当前 verifier 已覆盖 `MIG-CELL-INSTALL` 或五个 supported cell 的 target closure，不证明 target implementation、fresh semantic review、provider execution 或 Requirements gate 通过 |
| `EVO-EVD-035` | `REQ-REV-107..108` | `2026-08-26` 本轮 deterministic pre-review 对 preset workflow 文案、migration 行序及 cutover 前后 failure 分类的复核结果 | 证明 Requirements/导航已按 provider 事实统一 preview/force、substep 顺序与 composite terminal 语义 | 不证明 fresh semantic review、provider execution、target implementation 或 Requirements gate 通过 |
| `EVO-EVD-036` | `REQ-REV-109` | `2026-08-26` fresh Requirements 复核与本轮用户产品约束：distribution target 必须先分类 clean/partial/legacy/mixed，Intake 的 Architecture applicability 延后到 pre-design Planning，migration cell 只按适用性执行且全 `not_applicable` 仍须形成 composite current | 证明主需求、状态/回程投影、NFR、inventory、fixture 与 task-local mapping 已按同一分类、单次 RDT/Architecture 承接和 composite migration 语义修订 | 不证明 provider mutation、target implementation、fresh gate 通过或 Design readiness |
| `EVO-EVD-037` | `REQ-REV-110..115` | `2026-08-26` 对 fresh `origin/main=d907fcc5e17f23b6499648e5e9a208457f2d6f8b` 的 immutable reconciliation；`.40` RDT/Architecture authority 与 21 active Skills/89 exits 保持，`82fb5172`/`f8d8b20d` 增加相关 Gitlink reviewed-content identity，`73973273` 增加 EOF-only non-blocking observation；同轮 stock 全稿复核确认 role target 为 `9/1/2/5` | 证明 current authority、`CUR-CAP-022..023` 与 before-dev/update-spec/meta suppression、channel implement 双 profile 的 Requirements 修订依据 | 不证明 target suppression/adapter/worker 已实现，不证明 fresh Requirements gate 或 Design readiness |
| `EVO-EVD-038` | `REQ-REV-116` | `2026-08-26` 对 frozen `@mindfoldhq/trellis@0.6.15` stock source 与当前四类 role contract 的 fresh 复核：channel=transport，workers=caller-bound result，session-insight=history read；raw break-loop 的 source 会强制更新 spec/提交，不能作为 target explicit write owner | 证明 current Requirements 必须只保留 break-loop 的显式只读诊断/建议，并把任何写入意图在 raw invocation 前交回 Guru lifecycle；同时证明 provider/worker/explicit/Guru successor 需要使用互斥能力词汇 | 不证明 suppression、adapter、projection 或 worker 已实现，不证明 fresh Requirements gate 或 Design readiness |
| `EVO-EVD-039` | `REQ-REV-117..119` | `2026-08-26` 本轮 fresh Requirements/Strict technical review 对 distribution preclassification、hook setup matrix 与 guard reuse 的 current-candidate 复核 | 证明三项局部 Requirements 缺口已按正向模型修订并保持 Design/implementation 边界 | 不证明修订后 fresh full-document gate、fixture execution、provider mutation、implementation 或 Design readiness |
| `EVO-EVD-040` | `REQ-REV-120..121` | `2026-08-26` 对 frozen `@mindfoldhq/trellis@0.6.15` init/file-writer/workflow configurator 的 fresh 复核：`-y` 与 `--skip-existing` 均使用 skip 写模式，official init 管理 workflow 之外的 config/scripts/spec/task/workspace/platform surface；同轮 strict review 复核 byte-equal observation 的有序 cutover 语义 | 证明 official Trellis-only footprint 必须进入 existing migration，并证明 invocation-entry byte equality 不得提前改变前序 substep terminal 分类；当前 Requirements/NFR/inventory 已同步该边界 | 不证明 provider mutation、target implementation、fixture execution、fresh full-document gate 或 Design readiness |
| `EVO-EVD-041` | `REQ-REV-122..124` | `2026-08-26` 对当前 exact candidate 的 Requirements/Strict technical 复核：migration state projection、explicit-only caller/result table 与 capability-loss/consistency terminology 三项跨文档对读 | 证明三项局部合同已分别同步到 main state/return、NFR quality wording 与 inventory caller/result/closure，并保持 `requirements_draft` | 不证明 fresh full-document gate、fixture execution、provider mutation、implementation 或 Design readiness |
| `EVO-EVD-042` | `REQ-REV-125` | `2026-08-26` 当前对话中的正常 Guru workflow 曾在 current 副作用计划后将 `确认执行 <hash>` 作为继续条件；live `.trellis/workflow.md`、Commit/Merge Skill 与 `guru-qualify-normal-scenario:requirements_scope_set` 复核结果为 `classified / qualified_current` | 证明 semantic confirmation 是 current capability preservation 与真实 normal-path defect，不是 attack hardening；Requirements 必须拥有完整 plan/semantic affirmative/stale/scope/READY-merge/script boundary 与 zero-count 验收 | 不证明 fresh Requirements/Design gate、fixture execution、implementation、commit、push、PR、merge、Release 或 cleanup 已发生 |
| `EVO-EVD-043` | `REQ-REV-129` | `2026-08-27` 用户明确要求以 repository RDT SSOT、Architecture Baseline SSOT 与 task 三件套形成稳定可缓存主上下文，并由 AI owner 自主判断和执行；`guru-qualify-normal-scenario:requirements_scope_set` 对 cache-first authority context 与 AI-owned autonomous progression 均返回 `classified / qualified_approved_expansion` | 证明该原则属于 #305 既有低成本执行与 authority-integration 范围，必须形成 stable locator/identity/order、minimal live tail、cache-non-authority 与零事务交接/事实复述合同 | 不证明 runtime cache hit、相对性能提升、fresh Requirements/Design gate、implementation 或任何 Git/GitHub 副作用已发生 |
| `EVO-EVD-044` | `REQ-REV-133..136` | `2026-08-27` 用户明确决定先完成 #311/#312 再开始 Evolution，并要求重构后不得丢失两项能力；live Issue authority 固定 installed provenance source/target separation 到完整 publication terminal continuity，以及 original-worktree clean-tracked continuity、unrelated-dirty isolation 与真实 blocker 的 observable contract | 证明两项是 Evolution prerequisite/current-capability preservation 输入，要求 exact merge/base rebind；requirement/normal-path fixture 零差集与 merge-bound fresh Requirements review 只放行 Design，随后 Design successor/mapping 零差集与 fresh Design review 才放行重构；#311 形成 mandatory `TARGET-DELTA-014`，#312 当前投影折入既有 `CUR-CAP-012`，二者均须在 merge 后 fresh reclassify | 不证明两项已 merge/current，不证明任何 Design owner/DTO/Skill、runtime implementation、base sync、review pass 或 Git/GitHub 副作用 |
| `EVO-EVD-045` | `REQ-REV-133..136` | `2026-08-28` live selected-base reconciliation：`origin/main=3efcce72…` 已包含 PR #313/merge `21c7da1…`（#311）与 PR #314/merge `3efcce7…`（#312）；#311 accepted implementation scope 与 exact merge/reachability current，Issue `OPEN` 仅保留 production release、错误文件重试和 Issue closure follow-up；#312 accepted scope 与 exact merge/reachability current，Issue `CLOSED` | 证明 prerequisite 必须分别评估 accepted scope、exact merge identity、merge reachability、accepted-scope findings、Issue lifecycle 与 `open_followup_only`；当前 merge/lifecycle 维度已重绑定，但 inventory/RDT/Architecture fresh rebind、merged behavior reconciliation、requirement/normal-path zero-loss 与 fresh Requirements gate 仍是 Phase 1 阻断 | 不证明 Requirements gate、Design successor、runtime implementation、base sync、commit、push、PR、merge、Release 或 cleanup 已完成 |
| `EVO-EVD-046` | `REQ-REV-137..138` | `2026-08-29` 对 selected base `.41` authority 的 fresh reconciliation：`REQ-047..051`、`TST-031..035`、`SCN-041..047` 明确 installed publication source/target、完整 terminal continuity、installed distribution isolation 与 evidence-before-cleanup standalone verifier failure contract | 证明 #311/#312 已可作为 selected-base current capability 分类，并要求 83/13/50 target successor closure；#311 不再重复建立 target delta | 不证明 fresh Requirements review、Design successor、runtime implementation 或发布已完成 |
| `EVO-EVD-047` | `REQ-REV-137..138` | `2026-08-29` current-to-target set comparison 与 projection sync：23 个 `CUR-CAP-*`、13 个 `TARGET-DELTA-*`、83 个 `EVO-REQ-*` 和 50 个 `EVO-FIX-*` 均有 Requirements-stage successor；verifier failure fixture 与 installed publication fixture 互相独立 | 证明 authority rebind、merged-behavior reconciliation 与 Requirements-stage zero-loss current，可进入 fresh Requirements 双审 | 不证明双审已通过、Design 已更新、fixture 已执行或任何 Git/GitHub 副作用已发生 |
| `EVO-EVD-048` | `REQ-REV-139` | `2026-08-30` live `origin/main=a41b8a34d237e1863225d069ca9c6b5ad6ae476a` 已激活唯一 current `.42` RDT/Architecture authority；extension candidate 为 `0.6.15-guru.39`、target Release 为 `v0.6.15-guru.3`，tag/Release/smoke 仍 `unverified`；`.41...42` 语义 diff 只新增 release-authority identity、traceability、evidence 和 promotion lifecycle facts | 证明 #305 必须 fresh 重绑 current authority，并可在不改变 23 current capabilities、13 target deltas、50 fixtures、21 active Skills 或 89 exits 的前提下恢复 Requirements/Design gate | 不证明 `.3` 已发布、#311 post-release proof 已完成、Evolution runtime 已实现、base 已同步或任何 Git/GitHub 副作用已发生 |
| `EVO-EVD-049` | `REQ-REV-140` | `2026-08-30` live `origin/main=9f560ec191851f82768d5e7aa031e6d852c34f14` 相对 `a41b8a34…` 仅包含 `f5c47d28…` #267 lifecycle-evidence fix、`f7c5866e…` dogfood provenance、`7d7db281…` archive 和 `9f560ec1…` PR #316 merge；extension/CLI version 与 `.42` product/runtime authority 不变 | 证明 #305 必须再次 fresh 重绑 exact selected-base identity，并可在 52/83/33/23/13/50/73/21/89 集合全部不变的前提下恢复 Requirements/Design gate | 不证明 base 已同步、runtime 已实现、tag/Release/smoke 或 #267/#311 post-release proof 已完成，也不授权任何 Git/GitHub 副作用 |
| `EVO-EVD-050` | `REQ-REV-141` | `2026-08-30` live `origin/main=736ef3335f1b1b0dcbf92f1e8e53343f922aa32a` 经 PR #317 合入 installed Finalizer platform-selection preservation：三处 selected lists 必须 non-empty/sorted/unique/canonical/identical，`all_platforms=true` 只接受完整 `claude,codex,cursor` 并映射 `--all-platforms`，其余合法 selection 映射 ordered repeated `--platform`；invalid identity 在 extension source checkout、preset apply 与 provenance commit 前 fail closed。latest stable current fact 同时更正为 `v0.6.15-guru.2` / extension `.38` / CLI `0.6.15`，`.3/.39` 仍 unverified | 证明 #305 必须把 platform-selection preservation 作为 material current capability advance 同步到 `CUR-CAP-013/014/017`、`EVO-REQ-084`、`EVO-NFR-034` 与既有 installed-publication fixture，并重做 fresh Requirements/Design gate；84/34 增号但 52/23/13/50/73/21/89 集合保持 | 不证明 Evolution runtime 已实现、`.3/.39` 已发布、fresh Requirements/Design review 已通过、base 已同步或任何 Git/GitHub 副作用已发生 |
| `EVO-EVD-051` | `REQ-REV-142` | `2026-08-30` live `origin/main=5650df47fe17fe89b7cb616be6c9551608164832` 经 PR #318 合入 #267 Finalizer caller-inventory consistency 修复：唯一 second-hop row 只更新 `id` / `anchor_sha256`，Issue Scope Ledger 保持 related-only，随后仅更新 dogfood provenance、task archive 与 merge facts；PR body 明确 runtime、public Skill API/schema/typed exit、Release identity、extension revision 与 `.42` authority 均不变 | 证明 current locator 必须从 `736ef333…` 重绑到 `5650df47…`，但该 delta 只能分类为 fact-only；52/84/34/23/13/50/73/39/43/37/21/89 与 #311/#312/#317 preservation 原样保持，旧 exact-base review 不得复用 | 不证明 base 已同步、Evolution runtime 已实现、`.3/.39` 已发布、fresh Requirements/Design review 已通过或任何 Git/GitHub 副作用已发生 |

The Requirements gate consumes these as research evidence only. A `STOCK-HANDOFF-*` or
`upstream_suppression_blocked` branch is a complete Requirements result when its owner tuple,
projection cell, real current direct consumer or explicit handoff projection, and repair input are
explicit; it is not a claim that the Design owner already exists.

## Acceptance Mapping

- `EVO-001..007`、`EVO-CAP-001..004`：由 repository Requirements 主定义及 Goal trace 审核。
- Finding 严重度只使用 `finding_severity=P1/P2/P3`；核心能力表中的 `P0/P1/P2` 只表示
  `capability_priority`，不引入或混用 `P0` finding。
- `REQ-UC-EVO-001..052`、`EVO-REQ-001..084`、`EVO-NFR-001..034`：执行编号完整性、
  入口分支、唯一主定义与 fixture 可达性差集检查。
- `.42` current capability：校验 selected-base live 21 active Skills / 89 external exits，并审核
  `CUR-CAP-001..023`、previous Skill coverage、post-`.41` Gitlink/EOF current delta 与 Test capability successor closure；`.40`/`d907fcc5…`
  只作为历史 comparison evidence；另外独立验证
  `workflow/task_data/docs_authority` capability-loss 与 Skill API/distribution/installed
  consistency，禁止把后一类 drift 归类为 capability loss。
- Prerequisite/current-to-target closure：`CUR-CAP-001..023`、`TARGET-DELTA-001..013` 与 50 个
  Requirements fixture 均可达；当前 selected base 已包含 #311/#312 merge，并已按六维 prerequisite
  classification 从 exact base fresh rebind、执行 merged-behavior reconciliation 与零差集比较。#311/#312
  使用既有 current-capability successor，不新增 `EVO-CAP-005`、`TARGET-DELTA-014` 或更高 delta。
- Stock closure：17 个 logical stock asset（`suppressed=9`、`provider=1`、`explicit=2`、
  `worker=5`）、一次 shared `.agents/skills` projection layer、三个 supported Guru host、每个
  host 的五类 invocation context、Codex/Claude/Cursor 的 hook configuration 与 `EVO-REQ-080`
  七个互斥 setup discriminator cell、九个 host-bound retained
  nonsemantic row 与两个 stock fixture 必须完成 exactly-one role/successor/caller/profile/
  typed-result/real consumer or explicit handoff projection/projection-cell/Design-handoff/action/provenance closure；明确的
  `consumer_unbound`/`current_drift` 只能作为 Design handoff，不得被算作 current closure；
  package/source identity 与 projection cell identity 按所选 action 绑定；未闭合项阻断 final gate。
- Clean/migration closure：clean fixture 必须从无任何可归属 official Trellis/Guru managed installation
  state 的
  初始 repository 绑定 immutable source/candidate、`trellis/index.json` identity、fresh-init/preset
  provider stimulus、application/validation current、created resources、shared/host projection cells、
  executable bit 与 exact-step blocked/re-entry；existing migration 必须在一个 composite invocation
  中按 `MIG-CELL-INSTALL`、`MIG-CELL-UPGRADE`、`MIG-CELL-UPDATE`、`MIG-CELL-WORKFLOW-SWITCH`、
  `MIG-CELL-PRESET-REAPPLY` 的固定顺序逐一判定 applicability，只执行适用 substep；全部
  `not_applicable` 仍形成 composite current 并执行一次 final validation。每个 cell 独立证明自己的
  applicability 与 initial state；适用 cell 还须证明 exact provider stimulus、provider flag/profile
  discriminator、source/target、step phase、owner、受影响 sidecar 与 step-local blocked/re-entry；
  不适用 cell 记录可判定原因，不冒充 provider execution。共享 composite context 只建立一次并盘点
  active/history/retained-ref、RDT/Architecture authority 与唯一 cutover。workflow switch 必须是
  `--create-new` preview 后显式 `--force` application；该 force write（或在固定顺序抵达
  WORKFLOW-SWITCH boundary 后对 byte-identical target 的 current 观测）就是唯一 cutover，invocation-entry
  byte equality 不提前改变前序 substep 分类，PRESET-REAPPLY 和一次 final validation 位于其后。update 的
  `--force`/`--create-new` 只能是独立
  control subcell，不能替代 `--skip-all` canonical 分支；不能以“同上”或一个 substep 的结果代替
  其它 substep。WORKFLOW-SWITCH preview/preflight 及其之前的 finding 由 composite
  `pre_migration_current_preserved` result 证明 pre-migration current，并可在未执行 final validation
  时停止。进入 cutover 后必须完成 PRESET-REAPPLY，并只在全部适用 substep current 后执行一次 final
  validation；它证明 post-cutover preservation 与 legacy-consumer absence。cutover 后的
  WORKFLOW-SWITCH、PRESET-REAPPLY 或 final-validation finding 按 live state forward recover，并只产生
  `new_contract_current` 或 `migration_blocked`；final validation current 前 target consumer 调用数为 0。
  三类是唯一 composite terminal，substep result 不得冒充完整 migration terminal。
- Fixture boundary：`EVO-FIX-STOCK-COEXISTENCE` 是 admission-only fixture，只能验证
  lifecycle/source classification、redirect 或 role-local fail-closed、passive context 不消费请求，
  不得执行或计入任何 patch、absence/removal、quarantine、delete 或其它 mutation；实际 mutation
  只由 `EVO-FIX-STOCK-MAINTENANCE` 在已选 action 且 caller/scope/target/provenance 绑定后验证，
  并独立覆盖 pending/partial/unknown、user-modification、sidecar 与 blocked/re-entry。Admission
  通过不能推导 maintenance mutation 已验证，反之亦然。
- Current Phase 1 gate：当前 merge/lifecycle、authority rebind、merged behavior reconciliation 与
  requirement/normal-path fixture zero-loss projection 已同步；fresh Requirements 全稿双审与确定性闭包已针对
  `origin/main@5650df47…` 通过，因此 `requirements_ready_for_design` / `requirements_trace_ready_for_design` 是 current 状态。
  Design successor/mapping、fresh Design review 与确定性闭包也已针对同一 exact candidate 完成，
  `design_ready_for_delivery_planning` / `fresh_design_review_passed` / `evolution_refactor_eligible` current；当前
  continuation 明确停在该 Phase 1 gate 后，不进入实现。

## Fresh Requirements Gate

- `REQ-REV-011..142` 的 current 正文修订已完成；`REQ-REV-133` 使绑定旧 base/能力清单的 Requirements 与
  Design review 结论 stale，`REQ-REV-141` 又使 pre-PR-#317 Requirements/Design gate stale，`REQ-REV-142`
  使绑定 pre-PR-#318 exact base 的 Requirements/Design gate stale；任何旧 gate、
  snapshot 或局部 PASS 都不得复用。
- 当前 Requirements 主定义、NFR、核心能力、fixture、inventory、导航与 task/Design/Test/Architecture
  target projection 已同步为 52 UC / 84 REQ / 34 NFR / 23 current capabilities / 13 target deltas / 50 fixtures，
  并绑定 selected-base `5650df47…` 与 `.42` authority；`.42` additions 与
  `a41b8a34...9f560ec1` lifecycle/provenance/archive delta 为 fact-only，`9f560ec1...736ef333` 的 platform-selection
  advance 则经 `EVO-REQ-084` / `EVO-NFR-034` 折入既有 capability/fixture；`736ef333...5650df47`
  caller-inventory/provenance/archive delta 为 fact-only。
- 本 continuation 已对该 exact candidate 完成 fresh Requirements semantic、Strict technical 与确定性闭包
  审核；P1/P2/P3 finding 与 high-risk open question 均为 0，current gate 为
  `requirements_ready_for_design` / `requirements_trace_ready_for_design`。
- Design/Test/Architecture/task planning projection 已同步；当前 continuation 随后完成 fresh Design 全稿审核与
  确定性闭包，`design_ready_for_delivery_planning` / `fresh_design_review_passed` /
  `evolution_refactor_eligible` current，但仍未进入实现或 delivery execution。
