{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "guru-team-skill-evals-1.0",
  "schema_version": "1.0",
  "skill_name": "guru-clarify-requirements",
  "evals": [
    {
      "id": "clear-route",
      "prompt": "Review the staged fully answered requirement scenario, complete the semantic owner result, and invoke guru-clarify-requirements with its public input plus that private owner-result locator. Return the checker-derived route.",
      "expected_exit": "clear",
      "expected_output": "The public DTO is the declared clear route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "clear"
          },
          {
            "id": "public-only",
            "kind": "trace",
            "invariant": "public_invocation_only"
          },
          {
            "id": "evals-not-loaded",
            "kind": "trace",
            "invariant": "evals_not_loaded_by_skill"
          },
          {
            "id": "private-runtime-not-read",
            "kind": "trace",
            "invariant": "private_runtime_not_read_by_agent"
          }
        ]
      },
      "files": ["evals/files/eval-clear-input.json", "evals/files/eval-clear-facts.json"],
      "input_profile_id": "standalone_review"
    },
    {
      "id": "needs-context-route",
      "prompt": "Review the staged active-task scenario with named missing repository evidence, complete the semantic owner result, and invoke guru-clarify-requirements with its public input plus that private owner-result locator. Return the checker-derived route.",
      "expected_exit": "needs_context",
      "expected_output": "The public DTO is the declared needs_context route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "needs_context"
          }
        ]
      },
      "files": ["evals/files/eval-needs-context-input.json", "evals/files/eval-needs-context-facts.json"],
      "input_profile_id": "active_task_scope_change"
    },
    {
      "id": "refresh-context-route",
      "prompt": "Review the staged standalone scenario whose source binding changed, complete the semantic owner result, and invoke guru-clarify-requirements with its public input plus that private owner-result locator. Return the checker-derived route.",
      "expected_exit": "refresh_context",
      "expected_output": "The public DTO is the declared refresh_context route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "refresh_context"
          }
        ]
      },
      "files": ["evals/files/eval-refresh-context-input.json", "evals/files/eval-refresh-context-facts.json"],
      "input_profile_id": "standalone_review"
    },
    {
      "id": "retarget-context-route",
      "prompt": "Review the staged duplicate-selection scenario, complete the semantic owner result and exact confirmation, then invoke guru-clarify-requirements with its public input plus that private owner-result locator. Return the checker-derived route.",
      "expected_exit": "retarget_context",
      "expected_output": "The public DTO is the declared retarget_context route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "retarget_context"
          }
        ]
      },
      "files": ["evals/files/eval-retarget-context-input.json", "evals/files/eval-retarget-context-facts.json"],
      "input_profile_id": "initial_change_request"
    },
    {
      "id": "new-task-route",
      "prompt": "Review the staged independent-delivery scenario and confirmed side-effect-free draft, complete the semantic owner result, and invoke guru-clarify-requirements with its public input plus that private owner-result locator. Return the checker-derived route.",
      "expected_exit": "new_task",
      "expected_output": "The public DTO is the declared new_task route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "new_task"
          }
        ]
      },
      "files": ["evals/files/eval-new-task-input.json", "evals/files/eval-new-task-facts.json"],
      "input_profile_id": "initial_change_request"
    },
    {
      "id": "blocked-route",
      "prompt": "Review the staged refusal and unresolved load-bearing question scenario, complete the semantic owner result, and invoke guru-clarify-requirements with its public input plus that private owner-result locator. Return the checker-derived route.",
      "expected_exit": "blocked",
      "expected_output": "The public DTO is the declared blocked route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "blocked"
          }
        ]
      },
      "files": ["evals/files/eval-blocked-input.json", "evals/files/eval-blocked-facts.json"],
      "input_profile_id": "initial_change_request"
    }
  ]
}
