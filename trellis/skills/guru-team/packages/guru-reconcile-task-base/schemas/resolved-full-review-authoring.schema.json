{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "guru-reconcile-task-base-resolved-full-review-authoring-1.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["profile", "mode", "base_ref", "review_intent"],
  "properties": {
    "profile": {"const": "branch_review"},
    "mode": {"type": "string", "enum": ["workflow", "standalone"]},
    "base_ref": {"type": "string", "minLength": 1},
    "review_intent": {"type": "string", "enum": ["initial_review", "fresh_final_review"]}
  }
}
