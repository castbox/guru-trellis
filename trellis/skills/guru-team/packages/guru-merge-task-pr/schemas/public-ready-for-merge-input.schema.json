{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "guru-merge-task-pr-input-ready-for-merge-1.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "profile", "mode", "repo_ref", "pr_number", "pr_url", "expected_head_sha", "expected_base_branch", "expected_head_branch", "expected_close_issues"],
  "properties": {
    "schema_version": {"const": "1.0"},
    "profile": {"const": "ready_for_merge"},
    "mode": {"const": "workflow"},
    "repo_ref": {"type": "string", "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$"},
    "pr_number": {"type": "integer", "minimum": 1},
    "pr_url": {"type": "string", "pattern": "^https://github\\.com/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+/pull/[1-9][0-9]*$"},
    "expected_head_sha": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
    "expected_base_branch": {"type": "string", "minLength": 1},
    "expected_head_branch": {"type": "string", "minLength": 1},
    "expected_close_issues": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "integer", "minimum": 1}}
  }
}
