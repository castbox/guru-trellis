{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "guru-team-skill-evals-1.0",
  "schema_version": "1.0",
  "skill_name": "guru-discover-change-context",
  "evals": [
    {
      "id": "context-ready-route",
      "prompt": "Review the staged current-context scenario and complete the semantic owner result. Run the real Discovery record, check, and invoke wrappers with --invocation - using one closed semantic-owner 1.0 envelope containing schema_version, public_input, independent base_current transition, owner_context={}, and owner_result. Keep all input in memory; replace owner_result with recorder stdout before check/invoke, without prewritten input files. Return the checker-derived route and complete Clarify handoff.",
      "expected_exit": "context_ready",
      "expected_output": "The public DTO is the declared context_ready route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "context_ready"
          },
          {
            "id": "public-only",
            "kind": "trace",
            "invariant": "public_invocation_only"
          },
          {
            "id": "evals-not-loaded",
            "kind": "trace",
            "invariant": "evals_not_loaded_by_skill"
          },
          {
            "id": "private-runtime-not-read",
            "kind": "trace",
            "invariant": "private_runtime_not_read_by_agent"
          }
        ],
        "semantic": [
          {
            "id": "bilingual-current-evidence",
            "criterion": "The owner relates Chinese Docs term 发布门禁 to English code symbol release_gate and does not claim either mechanism absent after a one-language zero result.",
            "evidence_selector": "transcript"
          },
          {
            "id": "exact-literal-preserved",
            "criterion": "Exact error OBJECT_ACCESS_DENIED remains an original literal lookup and is not mechanically translated.",
            "evidence_selector": "transcript"
          }
        ]
      },
      "files": ["evals/files/eval-context-ready-input.json", "evals/files/eval-context-ready-facts.json"],
      "input_profile_id": "pre_task"
    },
    {
      "id": "refresh-base-route",
      "prompt": "Review the staged stale-base re-entry scenario and complete the semantic owner result. Use the closed semantic-owner 1.0 envelope via --invocation - for Discovery owner commands and public invoke, without public/transition/owner input files. Preserve the stale-base re-entry boundary; do not claim record succeeded if live authority is stale. Return the refresh route derived from the existing freshness checks.",
      "expected_exit": "refresh_base",
      "expected_output": "The public DTO is the declared refresh_base route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "refresh_base"
          }
        ]
      },
      "files": ["evals/files/eval-refresh-base-input.json", "evals/files/eval-refresh-base-facts.json"],
      "input_profile_id": "pre_task"
    },
    {
      "id": "blocked-route",
      "prompt": "Review the staged unavailable-evidence scenario and complete the semantic owner result. Transport the closed semantic-owner 1.0 envelope through --invocation - for the Discovery record/check/invoke wrappers without prewritten input files. Return the blocked route derived by the checker; never replace missing semantic evidence with transport success.",
      "expected_exit": "blocked",
      "expected_output": "The public DTO is the declared blocked route.",
      "assertions": {
        "deterministic": [
          {
            "id": "expected-route",
            "kind": "json_path",
            "pointer": "/exit_id",
            "operation": "equals",
            "expected": "blocked"
          }
        ]
      },
      "files": ["evals/files/eval-blocked-input.json", "evals/files/eval-blocked-facts.json"],
      "input_profile_id": "pre_task"
    }
  ]
}
