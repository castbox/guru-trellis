{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/castbox/guru-trellis/schemas/guru-task-finalization-semantic-review-input-3.0.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "skill_id", "review", "route"],
  "properties": {
    "schema_version": {"const": "3.0"},
    "skill_id": {"const": "guru-finalize-task"},
    "review": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "summary"],
      "properties": {
        "status": {"enum": ["passed", "reroute", "blocked"]},
        "summary": {"type": "string", "minLength": 1}
      }
    },
    "route": {
      "type": "object",
      "additionalProperties": false,
      "required": ["typed_exit", "consumer", "output"],
      "properties": {
        "typed_exit": {"enum": ["base_reconciliation_required", "publication_review_stale", "resume_finalization", "reprepare_required", "ready_for_merge", "blocked"]},
        "consumer": {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "id"],
          "properties": {
            "kind": {"enum": ["skill", "workflow", "stop"]},
            "id": {"type": "string", "minLength": 1}
          }
        },
        "output": {
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": false,
              "required": ["materialization"],
              "properties": {"materialization": {"const": "executor"}}
            },
            {
              "type": "object",
              "minProperties": 1,
              "not": {
                "type": "object",
                "additionalProperties": false,
                "required": ["materialization"],
                "properties": {"materialization": {"const": "executor"}}
              }
            }
          ]
        }
      }
    }
  }
}
