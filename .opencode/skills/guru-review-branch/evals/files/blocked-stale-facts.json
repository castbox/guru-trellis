{
  "scenario": "The semantic review cannot establish a safe continuation, so the owner records the blocked stop route while reviewed content remains current.",
  "owner_evidence": {
    "semantic_gate": "blocked",
    "checker_required": true,
    "checkpoint_lifecycle": "retire_after_successful_blocked_projection"
  },
  "owner_staging": {
    "recipe": "review-blocked"
  },
  "public_invocation": {
    "expected_entry_route": "blocked",
    "arguments": ["--input", ".trellis/.runtime/guru-team/evals/public-input.json"]
  }
}
