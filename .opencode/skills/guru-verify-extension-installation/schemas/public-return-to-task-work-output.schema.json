{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "guru-extension-verify-installation-output-return-to-task-work-1.0",
  "oneOf": [
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["exit_id", "mode", "task_ref", "finding_refs", "resume_target"],
      "properties": {
        "exit_id": {"const": "return_to_task_work"},
        "mode": {"const": "workflow"},
        "task_ref": {"type": "string", "pattern": "^\\.trellis/tasks/[A-Za-z0-9._/-]+$"},
        "finding_refs": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
        "resume_target": {"const": "phase-2"}
      }
    },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["exit_id", "mode", "task_ref", "finding_refs", "resume_target"],
      "properties": {
        "exit_id": {"const": "return_to_task_work"},
        "mode": {"const": "standalone"},
        "task_ref": {"type": "string", "pattern": "^\\.trellis/tasks/[A-Za-z0-9._/-]+$"},
        "finding_refs": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string", "minLength": 1}},
        "resume_target": {"const": "phase-2"}
      }
    }
  ]
}
