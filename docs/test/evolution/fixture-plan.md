# Evolution Fixture Plan

状态：`test_candidate_planned` / `design_ready_for_delivery_planning` / `fresh_design_review_passed` /
`evolution_refactor_eligible`; all rows remain `planned_not_executed`. `Owner` is the semantic/correctness owner; workers may only supply the
listed evidence. `Blocked/re-entry` is part of acceptance, not an optional negative test.

| Fixture | Owner | Candidate / evidence layer | Requirements -> Design | Blocked/re-entry | Acceptance summary |
| --- | --- | --- | --- | --- | --- |
| `EVO-FIX-ENTRY-ROUTING` | Admission + Request Router + Answer | invocation contract / semantic+integration | `EVO-REQ-010,034,052`; `EVO-DES-006..018` | lifecycle causality/identity failure -> `invocation-binding-blocked` / `binding_reentry`; post-receipt isolation failure -> `independent-request-isolation-blocked` / `isolation_repair_reentry`; routing block -> exact Section 7.1 owner profile; controlled-adapter block -> `direct-answer-provider-blocked` / Answer `provider_reentry`; terminal Answer binding block starts a new invocation | user-event, post-owner and post-remote Admission each create the required fresh identity/envelope and exactly one receipt before one of six routes; `lifecycle_bound` carries `bound_event_ref,event_sequence`, with the ref binding host event identity/session/current content and both fields projected directly to the registered exact owner; ambient history lookup and sequence-only binding are invalid; the registry contains exactly 37 owners including Admission/Route Request/Answer/Current Work/History, while both specialist Skills retain their original caller; the two secondary classifiers are unique; provider unavailable/access/incomplete still answers with exact unverified facts; conflated binding/isolation results, availability-triggered answer block, fake terminal continuation and pre-route side effects are zero |
| `EVO-FIX-REQUEST-STOP` | Request Router | invocation contract / integration | `EVO-REQ-010,034`; `EVO-DES-013,018` | active work -> Current Work, not stop | safe no-side-effect stop completes; next request has new identity/receipt |
| `EVO-FIX-ACTIVE-DISPOSITION` | Disposition | lifecycle state / semantic+integration | `EVO-REQ-034,067,081`; `EVO-DES-016,038..042,069` | stale/resource -> `repair_reentry`; irreversible remote -> `active-lifecycle-remote-convergence-wait` -> one registered remote owner's `remote_disposition_resume` -> `remote_reentry`; cleanup refusal with one retain/suspend choice -> `cleanup_refusal_choice_reentry`; refusal without one choice -> named choice wait -> `choice_reentry` | retain/suspend/no-op abandon/confirmed cleanup/remote convergence are mutually exclusive and queryable; the wait joins one `remote_outcome_ref` with the Disposition continuation without exposing it to the remote owner; cleanup refusal deletes nothing, is not blocked, and direct-choice versus later-choice re-entry cannot be interchanged or routed generically |
| `EVO-FIX-INTAKE-CLEAR` | Clarification | change context / semantic | `EVO-REQ-002..007`; `EVO-DES-019` | source conflict -> one clarification | sufficient source reaches requirements current with zero routine question |
| `EVO-FIX-INTAKE-REVIEWED-DESIGN` | Clarification + Plan | change/plan / semantic | `EVO-REQ-003,022`; `EVO-DES-019,025` | conflict -> explicit decision owner | reviewed design is preserved/reconciled, never silently rewritten |
| `EVO-FIX-INTAKE-UNCLEAR` | Clarification | change context / semantic | `EVO-REQ-003..005`; `EVO-DES-019` | ambiguity -> same owner | exactly one highest-value question per round until current |
| `EVO-FIX-NO-ISSUE` | Standard owner graph | task lifecycle / integration | `EVO-REQ-011,034..043`; `EVO-DES-019..037` | no Issue -> closure-not-applicable | full local/PR-selected route never fabricates Issue identity |
| `EVO-FIX-TASK-FREE` | Task-free Change | checkout candidate / semantic+runtime | `EVO-REQ-010..011,047,050..051`; `EVO-DES-015,022` | recovery/escalation/blocked -> exact owner | bounded edit/check/revision; no task planning/history; partial-work escalation precedes standard resources |
| `EVO-FIX-DETACHED-READ` | Sync + Discovery | repository state / runtime | `EVO-REQ-009,060`; `EVO-DES-020` | unsafe authority checkout -> Sync blocked | exact authority reads without mutating detached/dirty invocation checkout |
| `EVO-FIX-ARCH-NO-IMPACT` | Architecture | plan candidate / semantic | `EVO-REQ-013..014`; `EVO-DES-024` | incomplete authority -> bootstrap/repair | explainable no-impact creates no contribution/ADR |
| `EVO-FIX-ARCH-ALIGNED` | Architecture + Plan | plan candidate / semantic | `EVO-REQ-013,015`; `EVO-DES-024..026` | stale -> Architecture sync | first design writing consumes current baseline/constitution/change contract |
| `EVO-FIX-ARCH-CONFLICT` | Architecture | plan candidate / semantic | `EVO-REQ-016`; `EVO-DES-024,062` | conflict -> Plan decision | product intent preserved; conflict blocks approval until explicit resolution |
| `EVO-FIX-ARCH-INCOMPLETE` | Architecture + Bootstrap | repository authority / semantic | `EVO-REQ-017,061`; `EVO-DES-024,063` | missing/stale/conflict -> repair then full re-entry | no design draft consumes incomplete Architecture |
| `EVO-FIX-ARCH-NEW-DECISION` | Architecture | contribution candidate / semantic | `EVO-REQ-018..020`; `EVO-DES-062..063` | review/promotion pending -> same owner | task contribution and ADR candidate exist; shared current remains unchanged |
| `EVO-FIX-ARCH-REVISION` | Architecture | contribution candidate / semantic | `EVO-REQ-018..020`; `EVO-DES-062..063` | incomplete concern -> revision | before/after, owner, path, GAP, compatibility exit and principle tradeoff close |
| `EVO-FIX-ARCH-NO-ADR` | Architecture | plan/contribution / semantic | `EVO-REQ-014..015,018`; `EVO-DES-024,062` | false trigger -> revision | no-impact/alignment has no contribution; non-decision contribution has no ADR |
| `EVO-FIX-FRESH-EQUIVALENT` | Earliest affected owner | any candidate / runtime+semantic | `EVO-REQ-026,040`; `EVO-DES-011,031` | material difference -> scope freshness route | identity-only drift refreshes the owner projection without replaying unchanged semantic review; consumer same-source full reread is zero |
| `EVO-FIX-FRESH-SCOPE` | Earliest affected owner | any candidate / semantic | `EVO-REQ-026,040`; `EVO-DES-011,029,031` | material change -> owner re-entry | section owner performs one targeted reread; only affected result and downstream become stale |
| `EVO-FIX-TECH-REVISION` | Plan | plan candidate / semantic | `EVO-REQ-008,025`; `EVO-DES-025..027` | requirement changed -> Clarification | technical revision does not replay unchanged Phase 0 |
| `EVO-FIX-PLAN-NORMAL` | Plan + Approval | task plan / semantic+integration | `EVO-REQ-012..026,044..046,049,064`; `EVO-DES-009..011,023..027,067` | finding/scope/authority -> exact author/owner; missing/refused/stale compound activation/implementation-entry choice -> wait/planning terminal/fresh review; objective activation failure -> Approval recovery only with zero implementation write | applicable RDT -> Architecture -> task `prd.md`/`design.md`/`implement.md` is a stable locator/identity/order prefix and live facts/delta/unresolved items are the minimal tail; Clarification/RDT/Architecture own their disjoint authority subprojections while every semantic consumer uses the same applicable bound content; cache hit/miss/unavailable results are equivalent; one author and one AI approval decide directly from that context, with minimal task projections and zero assignment/signoff/transaction handoff/current-fact restatement; the sole exact confirmation plan displays task activation plus immediate entry into the approved implementation scope/allowed writes and excludes later delivery actions; a current deterministic activation closes directly as `approved -> guru-implement-task:initial`, with no public activation router/terminal or second routine confirmation; authorization persistence zero |
| `EVO-FIX-SEMANTIC-CONFIRMATION` | Exact side-effect-owning semantic Skill | dialogue/action boundary / semantic+integration | `EVO-REQ-081`; `EVO-DES-022,030,035..036,041,044..055,069,072` | incomplete plan, question/limit/change or material target/scope/fact drift -> zero side effect and exact owner redisplay/re-entry; explicit refusal -> exactly one named owner-local zero-side-effect output | exact-current Workspace reuse first proves repo/Issue/base/branch/worktree/task/path plus ownership/isolation current, then emits `workspace_current` with plan display, confirmation wait, refusal branch and mutation all zero; only pending creation/transfer/isolation enters confirmation. After an unchanged complete exact plan, “确认继续”, “可以，继续” and other clear affirmatives each advance only the displayed action; the Planning plan alone is a declared compound action and must display both task activation and immediate approved implementation entry/allowed writes while excluding Commit and all later delivery actions; it produces no second routine confirmation and transition failure produces no implementation write. Provenance-tail preparation, branch push and PR creation each use their own prepare/wait/confirmation re-entry/refusal/recovery, no confirmation can authorize the next action, and exact already-current tail/remote branch/READY PR paths request no mutation confirmation; READY merge needs no fixed `合并PR`; base sync/Workspace/plan activation+implementation entry/Commit/provenance-tail/branch push/PR creation/merge/manual closure/Finish/delivery-terminal Cleanup/active-disposition cleanup/clean install/pre-cutover migration/post-cutover migration/stock/Release refusal outputs are disjoint and never masquerade as blocked; Clean/Migration/standalone Stock refusal continuations re-enter only their exact action owners, while true terminal refusals start later work through a new invocation/current-work route; fixed prompt/password/hash/digest/task/path/branch/SHA/identity/summary/prescribed-wording challenges, `确认执行 <hash>`, script parsing and authorization persistence counts are zero |
| `EVO-FIX-BRANCH-FINDING` | Check/Implementation/Commit/Review | committed range / semantic+runtime | `EVO-REQ-028..031`; `EVO-DES-028..031` | finding -> exact earliest owner | revision gets fresh check/commit/full-diff review; Gitlink and EOF-only rules preserved |
| `EVO-FIX-BASE-EVOLUTION` | Reconcile Base | task/base pair / semantic+runtime | `EVO-REQ-032,082`; `EVO-DES-032,071` | impact -> plan/implement/review/scope owner; real workspace boundary -> exact blocker owner | no task recreation; unrelated delta avoids full invalidation; #312 normal path allows current-base-tracked path-clean same-task files in the original active worktree. Source/task-worktree unrelated dirty remains owned and unchanged without same-task misclassification; dirty/untracked same-task files, review/check metadata and cwd/runtime/worktree/task/branch mismatch still block and cannot be bypassed by source-clean allowance |
| `EVO-FIX-EVOLUTION-PREREQUISITE` | Requirements prerequisite gate, then Design/refactor eligibility gate | issue/merge/base/RDT/Architecture/inventory identities / semantic+live authority | `EVO-REQ-082..083`; `EVO-DES-060..063,070..073` | each of the six dimensions is recorded independently: `accepted_implementation_scope`, `exact_merge_identity`, `merge_reachability`, `accepted_scope_findings`, `issue_lifecycle` and `open_followup_only`; an incomplete accepted scope, unresolved accepted-scope finding, unverifiable exact merge identity, unreachable merge, or unclear `OPEN` follow-up boundary -> `evolution_prerequisite_blocked` with Design/runtime actions zero; `issue_lifecycle=OPEN` with `open_followup_only=true` and `CLOSED` are not blockers by themselves; authority/reconciliation/requirement/normal-path fixture gap or Requirements finding remains blocked; after Requirements ready, Design successor/mapping gap or Design finding keeps runtime blocked | Current positive classification binds #311 PR #313/merge `21c7da1…` reachable + `issue_lifecycle=OPEN` + `open_followup_only=true` and #312 PR #314/merge `3efcce7…` reachable + `issue_lifecycle=CLOSED` + `open_followup_only=N/A`; `.41` RDT/Architecture/capability rebind, merged-contract reconciliation, 83/13/50 Requirements-stage successor zero gap and fresh Requirements review are current, including separate installed-publication and verifier-failure paths, so `requirements_ready_for_design` is established. The complete Design successor/fixture mapping, fresh Design review and deterministic closure are also current, so `evolution_refactor_eligible` is established. Old snapshot/review/local PASS reuse, treating OPEN as unmerged, Design-before-Requirements-ready and runtime-before-refactor-eligible counts are zero |
| `EVO-FIX-ARCH-PROMOTION` | Architecture + Post-review Authority Router + Repository Bootstrap | reviewed committed range / semantic+SSOT | `EVO-REQ-020,031`; `EVO-DES-031,063..064` | expected-current/contribution/projection drift -> same authority or Bootstrap sync; multi-authority outstanding plan -> ordered next owner | promotion/projection discriminators contain only work not already current at its reviewed target identity; RDT then Architecture serialize when both are outstanding; `authority_only\|with_code_spec` invokes Bootstrap-only locator/usage projection refresh after every selected promotion, if any, is current, then fresh Check/Commit/full-diff review. The normal matrix explicitly includes shared RDT/Architecture already current + missing/stale authority locator/usage/freshness + no same-range code-spec contribution -> `promotion_kind=none,projection_kind=authority_only`, with no promotion/code-spec ref and one reachable Bootstrap repair. That review sees the complete original diff but emits exact `promotion_kind=none,projection_kind=none` when authority and projection targets are already current; material drift reopens only the affected work |
| `EVO-FIX-ARCH-DOWNSTREAM-FRESHNESS` | Architecture | task lifecycle / integration | `EVO-REQ-026,031,040`; `EVO-DES-024,029,031,063` | material current change -> earliest owner | current binding reaches implementation through acceptance/Finish on both routes |
| `EVO-FIX-RDT-DOWNSTREAM-FRESHNESS` | RDT + Repository Bootstrap | task lifecycle / integration | `EVO-REQ-062..065`; `EVO-DES-023,029,031,060..064` | material RDT change -> earliest owner; projection finding -> Bootstrap | contribution updates during implementation; stale RDT is never consumed downstream; authority locator or reviewed code-spec changes pass through the single projection owner and fresh committed review |
| `EVO-FIX-PARALLEL` | Admission + exact lifecycle owners | two task candidates / integration | `EVO-REQ-033`; `EVO-DES-008,031,033..042` | failure -> only affected task owner | A PR/B none in both completion orders; work/provider/history/cleanup isolated and shared current single-writer |
| `EVO-FIX-FULL-NORMAL` | Global workflow graph | task candidate / integration | `EVO-REQ-001..051,059..066,081`; `EVO-DES-001..042,060..070,072` | any finding -> declared earliest owner; every ordinary recoverable block -> one Section 7.1 profile | one standard request-to-cleanup path with bound lifecycle event identity/content delivered directly to one of 37 exact owners, while specialist calls retain original-caller ownership; current RDT/Architecture/task-doc stable prefix, three authority subprojection owners, minimal changing live tail, cache-unavailable equivalence, AI-owned direct judgment and zero human-style handoff/current-fact restatement; one displayed compound confirmation covers activation plus immediate approved implementation entry and closes directly to Implementation without authorizing delivery; exact-action semantic confirmation, separate provenance-tail/branch-push/Draft-PR waits, targeted validation and no broken handoff; applicable promotion returns through Bootstrap projection refresh and fresh committed review, then already-current authority/projection converges to double-none; GitHub delivery orders immutable-source provenance -> publication-head push -> Draft PR -> Finish summary/archive/archive-push/Ready -> archive-bound Merge -> Closure -> delivery terminal -> Cleanup, and every exact already-current stage converges without mutation or confirmation; every provider action owner and public input/output consumer is complete without generic frame/private lookup, and no terminal result has a fake continuation; distribution/stock branches remain separate fixtures |
| `EVO-FIX-NONE` | Delivery Route + Acceptance | reviewed task / integration | `EVO-REQ-037..043`; `EVO-DES-033..037` | provider failure cannot choose none | explicit none has zero GitHub I/O and complete local acceptance -> Finish -> closure-N/A -> delivery terminal -> Cleanup order; Acceptance emits only acceptance current, pre-Finish delivery-fact input/output count is zero, and successful Finish is the sole delivery-fact producer |
| `EVO-FIX-PROVIDER-RECOVERY` | Exact provider action owner | provider action / controlled+live boundary | `EVO-REQ-039..041,053..056`; `EVO-DES-034..036,044..048,066,072..073` | timeout/aggregate-partial/unknown/divergent live state -> exact action re-entry with continuation; refusal -> owner-local not-executed stop and only its declared continuation where the action transaction remains resumable | complete `provider_context` inventory binds Git Sync/task-free-or-standard-local-write/Commit/publication-tail/branch-Publish/Finish archive commit+push, GitHub Draft-PR Publish/Ready/Merge/Closure, Trellis clean-Install/existing-Migration, standalone verifier, Guru preset Install/Stock-Reapply/activation-validation, immutable Release stages and caller-fixed controlled adapters to one closed action owner/profile each; provenance, branch push, Draft PR and Finish each have disjoint recovery. Exact current metadata tail, remote branch, Draft/READY base-head PR, official archive/archive push and Ready state converge at their own stage without mutation/confirmation, but only Finish may emit archive-bound `ready_for_merge_ref` and only Merge may consume it; standalone verifier binds non-null safe failure evidence before cleanup and returns only to Projection `standalone_reentry`; Merge and Closure carry their own continuation; Release zero-completed/unknown progress and true partial use disjoint DTOs; atomic state is completed/pending/unknown, live reread precedes at most one safe retry, and no route fallback, refusal-as-defect or duplicate irreversible action occurs |
| `EVO-FIX-FINISH-RECOVERY` | Finish + Merge + Cleanup | delivery terminal / runtime+integration | `EVO-REQ-042..043,081`; `EVO-DES-036..037,069,072` | Finish/Merge/Cleanup block -> same owner only; refusal -> finish-not-executed, PR-not-merged or resources-retained | semantic Finish owns one summary/archive/archive-push/Ready transaction confirmation after Draft PR and before Merge; its executor records completed/pending/unknown transitions, never repeats completed work and alone emits archive-bound `ready_for_merge_ref`. Merge then uses its own confirmation/recovery, Closure forms the delivery terminal, and Cleanup uses only that terminal; retained refs/history survive every refusal or partial recovery and no confirmation crosses an action boundary |
| `EVO-FIX-HISTORY-RESUME` | Current Work + History | lifecycle/archive state / integration | `EVO-REQ-034,043,047,067`; `EVO-DES-016,037..042` | not-found/multiple/material stale or owner id outside the Section 3.1 registry -> producer recovery block; a valid owner enters only its exact `current_work_resume` | active resume and durable query stay distinct; minimal result works after cleanup; current-owner routing never uses a matching Skill/profile, task scan, private checkpoint or ambient lookup |
| `EVO-FIX-LATEST-INTENT` | Admission + current owner | lifecycle event binding / integration | `EVO-REQ-047,076`; `EVO-DES-006..008,039..040` | lifecycle binding and independent isolation use their distinct blocks/re-entry; remote disposition uses the named convergence wait | additive/override reaches the exact Section 3.1 owner through `bound_event_ref,event_sequence`; the ref preserves host event identity/session/current content and forbids ambient lookup. After a no-side-effect/local owner result only `post_owner_pending`, and after one of the four remote outcomes only `post_remote_pending`, may allocate the fresh identity/envelope/receipt before isolation |
| `EVO-FIX-LONG-OUTPUT` | Exact long-running caller | provider/worker stream / runtime | `EVO-REQ-044..049`; `EVO-DES-009..011,065..067` | invalid handle -> exact caller block | waits return delta only; stable RDT/Architecture/task-doc prefix is not reinjected with cumulative output, cache is never required for correctness, and cumulative stdout/context, transaction handoff and current-fact restatement counts are zero |
| `EVO-FIX-PROJECTION` | Projection Validator | source/installed candidate / projection | `EVO-REQ-052..055,083`; `EVO-DES-043,056..059,064,073` | standalone block or embedded finding -> owning profile; selected standalone stock action -> fixed Stock caller -> Projection `stock_policy_reentry`; standalone execution failure -> evidence-before-cleanup -> `standalone_reentry` | standalone current/blocked and embedded returned-to-caller exits are mutually exclusive; standalone maintenance has the sole `Projection -> Stock -> Projection fresh validation` path and no fifth distribution action or Stock-owned workflow terminal; standalone execution failure binds non-null safe failure evidence before temporary cleanup and preserves exact Projection re-entry; activation binds reference-manifest/adapter and code-spec-projection identities; shared/three-host/new-contract parity holds, one-writer ownership holds and loss/consistency gates stay separate |
| `EVO-FIX-INSTALLED-PROVENANCE-PUBLICATION` | Publish + Finish + Merge | immutable extension source + target reviewed checkout / installed+integration | `EVO-REQ-037,053,082`; `EVO-DES-035..036,070,072` | missing/mutable/mismatched source identity, source/target confusion or extra managed/task/sidecar/config diff -> Publish `provenance_reentry`; archive/Ready mismatch -> Finish `finish_reentry`; archive-bound merge mismatch -> Merge `provider_reentry` | release-installed business repo without canonical source tree resolves immutable extension source from installation manifest/managed-byte/source-commit identity, validates source and target lineage independently, changes only declared provenance fields, preserves reviewed-content identity and continues one fast-forward publication-head push, one Draft PR, Finish summary/official archive/archive push/Ready, schema-valid archive-bound `ready_for_merge`, then Merge; terminal reinvoke repeats no Git/GitHub/archive mutation. Self-hosted control passes through the same owners without an installed-source dependency |
| `EVO-FIX-VERIFIER-FAILURE-EVIDENCE` | Projection Validator | verifier execution + cleanup boundary / installed+integration | `EVO-REQ-083`; `EVO-DES-043,065..068,070,073` | matrix or postcheck failure -> evidence before cleanup -> standalone verifier blocked/exact `standalone_reentry`; embedded caller finding stays caller-owned | one matrix command failure and one matrix-external `postcheck_failure` each produce schema-valid non-null stage/cell/command/exit/bounded credential-safe tail/stdout-stderr hash-size evidence before temporary cleanup; cleanup then proceeds and exact Projection re-entry remains queryable. Null failure, wrong cell/stage, secret-bearing or unbounded tail, cleanup-first recording, Finalizer consumption and embedded-caller ownership takeover are zero |
| `EVO-FIX-CLEAN-INSTALL` | Clean Installer | clean target / installed | `EVO-REQ-053`; `EVO-DES-044,056..059` | blocked -> `step_ref,continuation_ref,repair_input`; refusal -> `step_ref,continuation_ref` action re-entry | only true clean target; official projection then ownership-4.0 validation, Guru policy and atomic activation; created/current resources remain truthful and stock mutation before ownership handoff is zero |
| `EVO-FIX-MIGRATION` | Existing Migrator | existing target / installed+integration | `EVO-REQ-054`; `EVO-DES-045..046,055,059` | pre-cutover preserve or post-cutover forward block -> repair re-entry; phase-specific refusal -> same-owner action re-entry | five ordered applicability cells; UPDATE dry-run selects exactly migrate+skip-all or skip-all; ownership 4.0 precedes stock mutation; one cutover/reapply/final validation preserves history and leaves legacy consumers zero |
| `EVO-FIX-RELEASE` | Release Candidate | immutable candidate / release_exact | `EVO-REQ-056,081`; `EVO-DES-047..048,069` | exact pending/partial/post-publish stage re-entry or published-unverified | pre-publish, clear dialogue-local affirmation, zero-completed/unknown progress, true partial publication and tag-pinned verification never reorder/fallback or share an invalid progress DTO; the complete `EVO-DES-069` challenge/parser/persistence zero-count set holds |
| `EVO-FIX-WORDING-EXPLICIT` | Wording Review | specialist scope / semantic | `EVO-REQ-057`; `EVO-DES-018` | blocked/revision -> unchanged caller continuation and exact `wording_*_reentry` | all nine caller profiles cover their allowed results; standalone review-only never edits and completes after pass/full revision report; active content change discards the old result, rebuilds full scope/rescan and returns only to its fixed caller; arbitrary caller id/ref count is zero |
| `EVO-FIX-QUALIFY-EXPLICIT` | Normal Scenario Qualification | specialist scope / semantic | `EVO-REQ-058`; `EVO-DES-018` | scope -> profile-fixed Clarification and original-caller re-entry; active mechanism/block -> exact owner; standalone mechanism revision/profile -> schema-invalid | all ten active profiles cover four results, while standalone covers only classified/scope-confirmation/blocked; a real scope choice returns the unchanged caller continuation, the original caller rebuilds the full candidate set and freshly qualifies, and clarification failure remains route-local blocked |
| `EVO-FIX-CHANGE-REQUEST` | Change Readiness | change context / semantic | `EVO-REQ-059`; `EVO-DES-021` | clarification/prerequisite/resolution -> exact owner | ready/active duplicate/completed duplicate/clarification/prerequisite/block are unique and pre-resource |
| `EVO-FIX-BASE-REFRESH` | Sync Base | selected base / runtime+semantic | `EVO-REQ-060`; `EVO-DES-019..020` | ambiguous/unsafe -> Sync block | authority checkout advances exactly; invocation checkout unchanged; current handoff follows |
| `EVO-FIX-SSOT-BOOTSTRAP` | Repository Bootstrap | repository authorities / semantic+SSOT | `EVO-REQ-061`; `EVO-DES-023..024,031,060..064` | partial/stale/conflict -> Bootstrap-owned repair/block, then exact authority-owner re-entry; material projection drift -> Bootstrap repair then same projection profile | RDT and Architecture requests carry distinct opaque caller continuations; Bootstrap returns authority current only to the originating owner and never infers origin from task/private/ambient state; `projection_refresh` is triggered only for outstanding reviewed promotion/code-spec projection work, returns already-current target without a write, and otherwise returns only to Check, with raw update-spec, repeated current write and second writer counts zero |
| `EVO-FIX-RDT-LIFECYCLE` | RDT + Repository Bootstrap | task/repository authority / semantic+SSOT | `EVO-REQ-062..065`; `EVO-DES-023,029,031,060..064` | false no-impact/stale/promotion conflict -> RDT owner; projection conflict -> Bootstrap owner | no-impact/aligned/delta/repair/contribution/review/promotion are truthful and serialized; only not-yet-current promotion is selected, then the sole minimal spec projection writer and fresh Check/Commit/Review converge to double-none without replaying already-current work |
| `EVO-FIX-SUBMODULE-BOUNDARY` | Context + Implementation/Check | parent/nested repositories / runtime | `EVO-REQ-066`; `EVO-DES-020,068` | explicit nested scope -> new repository invocation | unrelated Gitlink init/read/write/test zero; relevant reviewed identity preserved without staging authority |
| `EVO-FIX-STOCK-COEXISTENCE` | Admission + Stock Policy read | 17 assets/hosts/contexts / semantic+projection | `EVO-REQ-068..071,074,076,078..080`; `EVO-DES-012,014,031,049..054,057..058,064` | role-local suppression/provider/retained block -> exact admission/policy owner | 9/1/2/5 roles, one shared layer, seven-cell host setup and separate platform/channel implementation adapters; all nine suppressed rows share only `request_admitted -> guru-route-request:top_level` as admission success while their capability successors remain downstream; update-spec successor proves Bootstrap review-trigger-to-projection/fresh-review reachability, meta successor proves manifest precedence, standalone plus seven embedded caller profiles and active/new-change write routing; raw calls remain zero; R01..R09 each bind one of nine exact hook-policy/session/workflow-breadcrumb context owners with no public caller id; mutation zero |
| `EVO-FIX-STOCK-MAINTENANCE` | Stock Projection | selected actions / projection+installed | `EVO-REQ-071..075,077,079..080`; `EVO-DES-031,043,049..059,064` | pending/aggregate-partial/unknown/user edit/sidecar/refusal -> exact policy/action re-entry | each action is completed/pending/unknown and aggregate priority is unknown > partial > action-required; suppressed update-spec/meta cannot become current from absence alone and must validate their concrete projection/reference successor identities; embedded `returned_to_caller` uses disjoint current/action-required/action-partial/action-unknown/action-not-executed/caller-finding schemas with no aggregate optional fields, and only action-state variants carry continuation; adapter block returns synchronously to its fixed caller, which owns any public recovery (including Answer `provider_reentry`), with no caller ref; the standalone caller is exactly Projection and `stock_policy_current` returns only to its `stock_policy_reentry` for fresh full validation; standalone/embedded/retained exits stay disjoint, reapply returns only to Existing Migrator's ordered update/preset-reapply cell and never writes `.trellis/spec`, users/sidecars are preserved, successors reachable and raw graph absent |

Publish refusal acceptance is exact: provenance refusal emits
`publication_preparation_not_executed -> task_ref,acceptance_ref -> task-publication-preparation-not-executed`;
push refusal emits
`branch_push_not_executed -> task_ref,acceptance_ref,publication_head_ref -> task-branch-push-not-executed`;
Draft-PR creation refusal emits
`pr_creation_not_executed -> task_ref,acceptance_ref,published_head_ref -> task-pr-creation-not-executed`.
Each consumer is distinct, zero-side-effect and terminal for the current invocation; missing output fields, a shared
fallback consumer, provider-block classification or cross-action confirmation reuse all fail the fixture.

## Execution Boundary

Design-stage planning validation may confirm that these 50 identities, owners, mappings and columns are closed.
Requirements readiness is current and all three new rows have reviewed Design allocation; fresh Design review and
deterministic closure passed, so the allocation is delivery-planning authority and `evolution_refactor_eligible` is
current. Validation must not change any row from `planned_not_executed`. Each future result belongs to the exact
implementation slice/candidate that executed it.
