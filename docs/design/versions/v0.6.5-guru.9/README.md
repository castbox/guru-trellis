# Released Design History：v0.6.5-guru.9

状态：`released-history`；tag commit：`56b5f411e533b200e4d8685ca7a2ffb0c778a7f5`（`source_confirmed`）。

该 tag 的完整 design/capability inventory 未在 #266 从 tag source 单独恢复，状态为 `unverified`。不得用 current main 的 21-Skill inventory 代表该 release。
