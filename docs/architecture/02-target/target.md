# TARGET

- `ARCH-TGT-001`（accepted direction）：RDT 与 Architecture authority 通过 task impact/contribution/promotion 持续维护，`.trellis/spec` 始终是最小 projection。
- `ARCH-TGT-002`（accepted external target, `unverified`）：在专项 owner 验证后支持 Trellis `0.6.15` upgrade/update。
- `ARCH-TGT-003`（accepted release direction, `unverified`）：通过 replacement exact-candidate 与累计 Release Gate 后发布下一稳定版本。
- `ARCH-TGT-004`（inferred）：后续 Phase owner 重构可进一步减少跨阶段耦合；未有独立 accepted contract 前不约束 CURRENT。

TARGET 不表示已实现、已测试、已发布或已授权执行。
